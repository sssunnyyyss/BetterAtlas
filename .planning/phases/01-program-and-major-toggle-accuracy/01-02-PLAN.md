---
phase: 01-program-and-major-toggle-accuracy
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/src/lib/programVariantSelection.ts
  - frontend/src/components/course/CourseFilters.tsx
  - frontend/src/pages/Catalog.tsx
  - frontend/src/lib/programVariantSelection.test.ts
  - frontend/src/pages/Catalog.program-mode.test.tsx
autonomous: true
requirements: [PRGM-01, PRGM-02, PRGM-03, PRGM-04]
must_haves:
  truths:
    - "Program search in filters can select valid program families without BA-only exclusion."
    - "Major/minor toggle always picks a deterministic counterpart from available variants, preserving degree intent when possible."
    - "Catalog deep links with `programId` and `programTab` remain valid and stable after refresh or invalid-tab input."
    - "Program-mode course display applies a deterministic AI-summary relevance signal when summary data is available, with stable fallback when it is not."
  artifacts:
    - path: "frontend/src/lib/programVariantSelection.ts"
      provides: "Single deterministic selector and ranking contract for program-mode variant switching."
      exports: ["selectProgramVariant", "buildProgramSearchOptions", "canonicalizeProgramTab"]
    - path: "frontend/src/components/course/CourseFilters.tsx"
      provides: "Program option generation that preserves program families beyond BA-only major filtering."
      contains: "programOptions"
    - path: "frontend/src/pages/Catalog.tsx"
      provides: "Program-mode toggle wiring, URL canonicalization, and AI-summary-informed course relevance ordering."
      contains: "programTab"
    - path: "frontend/src/lib/programVariantSelection.test.ts"
      provides: "Unit regression tests for selector/fallback rules."
    - path: "frontend/src/pages/Catalog.program-mode.test.tsx"
      provides: "Integration-style regression tests for toggle behavior and URL stability."
  key_links:
    - from: "frontend/src/components/course/CourseFilters.tsx"
      to: "frontend/src/lib/programVariantSelection.ts"
      via: "program option derivation for dropdown entries"
      pattern: "buildProgramSearchOptions"
    - from: "frontend/src/pages/Catalog.tsx"
      to: "frontend/src/lib/programVariantSelection.ts"
      via: "major/minor toggle target selection"
      pattern: "selectProgramVariant"
    - from: "frontend/src/pages/Catalog.tsx"
      to: "search params programTab"
      via: "tab canonicalization effect"
      pattern: "required|electives"
    - from: "frontend/src/hooks/usePrograms.ts"
      to: "frontend/src/pages/Catalog.tsx"
      via: "useProgramAiSummary-driven relevance signal for program-mode course ordering"
      pattern: "ai-summary|highlights|summary"
---

<objective>
Wire catalog program-mode UI to deterministic variant selection rules, add AI-summary-informed course relevance signaling, and harden URL/deep-link behavior with focused frontend regressions.

Purpose: Ensure user-visible major/minor toggles and program mode URL/course display state behave predictably for selection, refresh, deep links, and summary-aware relevance.
Output: Shared frontend selector utility, updated filter/toggle wiring, AI-summary relevance application in program mode, and regression tests for rules and URL stability.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-program-and-major-toggle-accuracy/01-CONTEXT.md
@.planning/phases/01-program-and-major-toggle-accuracy/01-RESEARCH.md
@frontend/src/components/course/CourseFilters.tsx
@frontend/src/pages/Catalog.tsx
@frontend/src/hooks/usePrograms.ts
@packages/shared/src/types/program.ts
@.planning/phases/01-program-and-major-toggle-accuracy/01-program-and-major-toggle-accuracy-01-SUMMARY.md

<interfaces>
From packages/shared/src/types/program.ts:
```typescript
export type ProgramTab = "required" | "electives";
export interface ProgramSummary {
  id: number;
  name: string;
  kind: "major" | "minor";
  degree: string | null;
}
export interface ProgramVariants {
  programId: number;
  name: string;
  majors: ProgramSummary[];
  minors: ProgramSummary[];
}
```

From frontend/src/hooks/usePrograms.ts:
```typescript
export function usePrograms(q: string): UseQueryResult<ProgramSummary[]>;
export function useProgramVariants(programId: number): UseQueryResult<ProgramVariants>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deterministic frontend selector utilities and option builders</name>
  <files>frontend/src/lib/programVariantSelection.ts, frontend/src/lib/programVariantSelection.test.ts</files>
  <action>Create a single utility module that centralizes: (1) family-preserving dropdown option derivation from program results (no BA-only gate), (2) major/minor toggle target selection with explicit ranking and degree-aware fallback, and (3) `programTab` canonicalization helper. Add unit tests covering deterministic ranking, counterpart selection, and invalid-tab handling.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/lib/programVariantSelection.test.ts</automated>
  </verify>
  <done>Utilities expose explicit deterministic behavior and tests prove ranking/fallback decisions are stable.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire `CourseFilters` and `Catalog` to consume selector utilities</name>
  <files>frontend/src/components/course/CourseFilters.tsx, frontend/src/pages/Catalog.tsx</files>
  <action>Replace inline BA-only filter logic in `CourseFilters` with utility-driven option derivation that preserves valid program families. Replace inline major/minor toggle sorting in `Catalog` with utility selection calls. Add/adjust state tracking for previous selections by kind as needed to preserve user intent on round-trip toggles. In `Catalog` program mode, evaluate existing `useProgramAiSummary` output (`summary` + `highlights`) and apply it as a deterministic relevance signal for displayed course ordering (signal as ranking boost, stable tie-break fallback, no visual redesign). Keep visual layout unchanged (correctness-only scope).</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Program option and toggle behavior are sourced from one deterministic selector utility; program-mode course ordering applies AI-summary relevance when available and remains stable when unavailable.</done>
</task>

<task type="auto">
  <name>Task 3: Add URL/deep-link regression tests for program mode</name>
  <files>frontend/src/pages/Catalog.program-mode.test.tsx</files>
  <action>Add frontend regression tests that mount Catalog in program mode and verify: selecting a program preserves valid variants, major/minor toggle lands on deterministic counterpart, and missing/invalid `programTab` canonicalizes to `required` without breaking course query behavior after refresh/deep-link. Add a focused test that when AI summary highlights are present, course ordering uses the expected relevance preference and remains deterministic across rerenders. Mock API hooks/client boundaries consistent with existing frontend test setup.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/pages/Catalog.program-mode.test.tsx</automated>
  </verify>
  <done>Program-mode tests fail on variant drift, invalid-tab URL behavior, or missing AI-summary relevance application, and pass with deterministic selector wiring.</done>
</task>

</tasks>

<verification>
- Frontend builds successfully with new selector utility and updated imports.
- Selector unit tests and Catalog program-mode regression tests both pass.
- Program-mode URL handling consistently canonicalizes `programTab` and preserves behavior after rerender/refresh simulation.
- Program-mode regression confirms AI-summary relevance signal influences course display ordering deterministically when summary data exists.
</verification>

<success_criteria>
- Users can find/select program families without BA-only filtering side effects.
- Users can toggle major/minor and receive deterministic same-family counterpart selection (degree-aware when available).
- Deep-linked and refreshed catalog URLs with `programId`/`programTab` remain stable and valid.
- Program-mode course display uses existing AI summary output as a bounded relevance signal without replacing deterministic toggle/variant behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-program-and-major-toggle-accuracy/01-program-and-major-toggle-accuracy-02-SUMMARY.md`
</output>
