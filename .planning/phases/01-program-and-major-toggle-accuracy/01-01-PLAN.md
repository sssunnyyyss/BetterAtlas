---
phase: 01-program-and-major-toggle-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/services/programService.ts
  - api/src/services/__tests__/programService.test.ts
autonomous: true
requirements: [PRGM-01, PRGM-02, PRGM-03, PRGM-04]
must_haves:
  truths:
    - "Program variant lookup returns major/minor candidates from the same program family using deterministic strict-first matching."
    - "Major/minor candidate ordering is deterministic and degree-aware so downstream toggles do not depend on arbitrary first-result picks."
    - "Program search/list data used by catalog selection does not hide valid active variants through unstable filtering."
  artifacts:
    - path: "api/src/services/programService.ts"
      provides: "Deterministic family/variant selection logic and stable ordering in program service queries."
      contains: "getProgramVariants"
    - path: "api/src/services/__tests__/programService.test.ts"
      provides: "Regression tests for strict-family matching and deterministic variant ordering behavior."
      contains: "describe(\"getProgramVariants\")"
  key_links:
    - from: "api/src/services/programService.ts"
      to: "getProgramVariants response"
      via: "strict-name subset selection with normalized fallback"
      pattern: "strict.*fallback|normalized"
    - from: "api/src/services/programService.ts"
      to: "frontend useProgramVariants query consumption"
      via: "stable majors/minors ordering in API payload"
      pattern: "orderBy|majors|minors"
---

<objective>
Define and implement deterministic backend variant-family selection so catalog program-mode toggles receive stable, relevant candidates.

Purpose: Eliminate unrelated fallback variants and non-deterministic ordering before frontend toggle wiring.
Output: Updated `programService` variant/list behavior and API-side regression tests that lock the contract.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-program-and-major-toggle-accuracy/01-CONTEXT.md
@.planning/phases/01-program-and-major-toggle-accuracy/01-RESEARCH.md
@api/src/services/programService.ts
@packages/shared/src/types/program.ts

<interfaces>
From packages/shared/src/types/program.ts:
```typescript
export type ProgramKind = "major" | "minor";
export interface ProgramSummary {
  id: number;
  name: string;
  kind: ProgramKind;
  degree: string | null;
}
export interface ProgramVariants {
  programId: number;
  name: string;
  majors: ProgramSummary[];
  minors: ProgramSummary[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten variant-family candidate selection in `getProgramVariants`</name>
  <files>api/src/services/programService.ts</files>
  <action>Refactor `getProgramVariants(programId)` to use a strict-first family rule: derive candidates by normalized program name, then prefer exact trimmed/lowercased name subset when that subset can satisfy both kinds. Only fall back to normalized-name candidates when strict subset is insufficient. Preserve existing out-of-scope boundaries (no ingestion pipeline changes, no UI redesign).</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Variant candidate set no longer relies on normalized-name-only matching when a strict same-family set is available.</done>
</task>

<task type="auto">
  <name>Task 2: Make backend variant ordering deterministic and degree-aware</name>
  <files>api/src/services/programService.ts</files>
  <action>Introduce explicit ranking helpers for major/minor ordering (exact degree affinity first, then stable lexical/id tie-breaks) and use them when constructing `majors` and `minors` arrays returned by `getProgramVariants`. Ensure list/search responses used by catalog selection avoid unstable or inactive-first records by adding/confirming active filtering and deterministic ordering in `listPrograms`.</action>
  <verify>
    <automated>pnpm --filter api test -- src/services/__tests__/programService.test.ts</automated>
  </verify>
  <done>Given identical data, `getProgramVariants` and `listPrograms` emit stable output ordering with no arbitrary first-item behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for strict-first matching and deterministic sorting</name>
  <files>api/src/services/__tests__/programService.test.ts</files>
  <action>Create focused service tests for: strict-family preference over broad normalized fallback, deterministic ordering across mixed degree variants, and preservation of major/minor grouping in response payload shape. Mock only required DB calls; keep assertions behavior-focused.</action>
  <verify>
    <automated>pnpm --filter api test -- src/services/__tests__/programService.test.ts</automated>
  </verify>
  <done>Tests fail on reintroduction of normalized-only family jumps or unstable ranking, and pass on the deterministic contract.</done>
</task>

</tasks>

<verification>
- API build/type-check succeeds after service refactor.
- New program service regression tests pass and are deterministic across repeated runs.
- Variant response keeps expected `ProgramVariants` shape consumed by existing frontend hook.
</verification>

<success_criteria>
- `GET /programs/:id/variants` returns same-family major/minor candidates with deterministic ordering on repeated calls.
- Backend selection logic no longer depends on arbitrary SQL ordering or first-record fallback.
- Automated tests codify strict-first family matching and degree-aware ordering behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-program-and-major-toggle-accuracy/01-program-and-major-toggle-accuracy-01-SUMMARY.md`
</output>
