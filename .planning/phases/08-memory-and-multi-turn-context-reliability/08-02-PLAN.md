---
phase: 08-memory-and-multi-turn-context-reliability
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - api/src/routes/ai.ts
  - api/src/routes/ai.memory-context.test.ts
  - api/src/ai/memory/sessionContextState.ts
  - api/src/ai/memory/topicShiftPolicy.ts
  - api/src/ai/grounding/sessionBlocklistState.ts
  - api/src/ai/grounding/sessionBlocklistState.test.ts
autonomous: true
requirements: [AIMEM-01, AIMEM-02, AIMEM-03]
must_haves:
  truths:
    - "Same authenticated user can run multiple chat sessions without memory or exclusion/blocklist bleed between session IDs."
    - "Reset clears only the targeted session context (memory + blocklist) and leaves other concurrent sessions intact."
    - "Topic shifts decay stale inferred recommendation constraints so new intent drives retrieval and recommendation generation."
    - "Latest-turn explicit constraints override older conversation context in final retrieval/filter decisions."
  artifacts:
    - path: "api/src/routes/ai.ts"
      provides: "Session-aware AI route orchestration that builds effective context from prompt + session state, applies topic-shift decay, and enforces latest-turn precedence."
      contains: "sessionId"
    - path: "api/src/routes/ai.memory-context.test.ts"
      provides: "Route-level memory reliability regressions for session isolation, reset scope, topic shifts, and latest-turn precedence."
      contains: "describe(\"POST /ai/course-recommendations memory context\")"
    - path: "api/src/ai/grounding/sessionBlocklistState.ts"
      provides: "Session-keyed blocklist state helper aligned with chat session keys (not user-only keys)."
      exports: ["getSessionBlockedCourseIds", "mergeSessionBlockedCourseIds", "clearSessionBlockedCourseIds"]
    - path: "api/src/ai/grounding/sessionBlocklistState.test.ts"
      provides: "Regression coverage proving blocklist TTL and clear behavior are isolated by session key."
      contains: "describe(\"sessionBlocklistState\")"
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/memory/sessionContextState.ts"
      via: "recommend schema/session key resolution drives memory read/update/clear calls per session"
      pattern: "resolveAiSessionKey\\(|clearSessionContext\\("
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/memory/topicShiftPolicy.ts"
      via: "route calls shift detector and precedence resolver before deriving search terms and active constraints"
      pattern: "detectTopicShift\\(|resolveConstraintPrecedence\\("
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/grounding/sessionBlocklistState.ts"
      via: "exclude/dislike persistence uses session key parity with memory context"
      pattern: "mergeSessionBlockedCourseIds\\("
    - from: "api/src/routes/ai.memory-context.test.ts"
      to: "api/src/routes/ai.ts"
      via: "in-process route tests assert cross-session isolation and latest-turn priority behavior end-to-end"
      pattern: "postRecommendation\\("
---

<objective>
Wire session-aware memory and topic/precedence policies into the AI route and lock behavior with dedicated route regressions.

Purpose: Complete AIMEM-01, AIMEM-02, and AIMEM-03 at the route layer so reliability is observable in real recommendation requests.
Output: Updated `/ai/course-recommendations` orchestration plus memory-context route test suite.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-memory-and-multi-turn-context-reliability/08-RESEARCH.md
@.planning/phases/08-memory-and-multi-turn-context-reliability/08-01-PLAN.md
@api/src/routes/ai.ts
@api/src/routes/ai.intent-routing.test.ts
@api/src/routes/ai.grounding-safety.test.ts
@api/src/routes/ai.relevance-calibration.test.ts
@api/src/ai/grounding/sessionBlocklistState.ts

<interfaces>
Current route request contract:
```typescript
const recommendSchema = z.object({
  prompt?: string;
  messages?: AiMessage[];
  reset?: boolean;
  excludeCourseIds?: number[];
  filters?: AiCourseFilters;
  preferences?: AiPreferenceSignals;
});
```

Current route branching and context derivation:
```typescript
const effectiveMessages = Array.isArray(messages)
  ? messages
  : [...getUserMemory(userId), { role: "user", content: prompt }].slice(-12);

const latestUser = [...effectiveMessages].reverse().find((m) => m.role === "user")?.content ?? "";
const decision = classifyIntent({ latestUser, recentMessages: effectiveMessages });
```

Target additions for this plan:
```typescript
sessionId?: string; // request body field
const sessionKey = resolveAiSessionKey({ userId, sessionId });
const context = sessionKey ? getSessionContext(sessionKey) : null;
// topic-shift + precedence applied before retrieval/filter assembly
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend route request/session contract and migrate memory + blocklist access to session keys</name>
  <files>api/src/routes/ai.ts, api/src/ai/grounding/sessionBlocklistState.ts, api/src/ai/grounding/sessionBlocklistState.test.ts</files>
  <action>Add optional `sessionId` to `recommendSchema` and derive a stable `sessionKey` using the new memory helper. Replace direct user-only memory calls with session-context reads/writes/clears so prompt-path requests use per-session state. Migrate blocklist helper usage to the same session key (including TTL and reset parity) so exclusions/dislikes are session-isolated. Preserve backward compatibility for callers that still send `messages` and for requests without `sessionId`.
</action>
  <verify>
    <automated>pnpm --filter api test -- src/ai/grounding/sessionBlocklistState.test.ts</automated>
  </verify>
  <done>Memory and blocked-course state are consistently keyed by session identity with reset scoped to the targeted session context.</done>
</task>

<task type="auto">
  <name>Task 2: Apply topic-shift decay and latest-turn precedence before retrieval and recommendation assembly</name>
  <files>api/src/routes/ai.ts, api/src/ai/memory/sessionContextState.ts, api/src/ai/memory/topicShiftPolicy.ts</files>
  <action>During recommend-mode preparation, compute latest-turn inferred constraints, run deterministic shift detection against prior session context, decay stale inferred constraints on shift, and resolve effective constraints using the precedence order from Plan 08-01. Ensure retrieval query terms, active filter composition, and recommendation assembly consume the resolved latest-turn-first constraint set. Keep Phase 6 grounding/filter safety and Phase 7 relevance policies intact.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Route behavior demonstrably prioritizes current turn intent/constraints and prevents stale prior-topic constraints from dominating recommendations.</done>
</task>

<task type="auto">
  <name>Task 3: Add dedicated memory-context route regressions for isolation, reset scope, shift decay, and precedence</name>
  <files>api/src/routes/ai.memory-context.test.ts</files>
  <action>Create a route-style Vitest suite for `POST /ai/course-recommendations` covering: same user with two `sessionId` values does not share memory/blocklist state; reset on one session leaves sibling session state intact; topic-shift prompts (e.g., "actually switch to ...") decay stale inferred constraints before recommendation generation; and latest-turn explicit constraints override older inferred context in retrieval/filter outcomes. Include one backward-compat test ensuring `messages` payload path still returns valid responses.</action>
  <verify>
    <automated>pnpm --filter api test -- src/routes/ai.memory-context.test.ts</automated>
  </verify>
  <done>Route-level regressions fail on session bleed, stale-constraint carryover, or latest-turn priority regressions, and pass for intended behavior.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` passes after session-aware route integration.
- `pnpm --filter api test -- src/routes/ai.memory-context.test.ts` passes for AIMEM-01/02/03 route behaviors.
- `pnpm --filter api test -- src/ai/grounding/sessionBlocklistState.test.ts` passes with session-key isolation updates.
</verification>

<success_criteria>
- Session context behavior is isolated per session key with explicit reset scope.
- Topic shifts reliably decay stale inferred constraints before recommendations are generated.
- Retrieval/filter decisions consistently prioritize latest-turn explicit constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/08-memory-and-multi-turn-context-reliability/08-memory-and-multi-turn-context-reliability-02-SUMMARY.md`
</output>
