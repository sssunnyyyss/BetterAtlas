---
phase: 08-memory-and-multi-turn-context-reliability
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - frontend/src/hooks/useAi.ts
  - frontend/src/features/ai-chat/hooks/useChatSession.ts
  - frontend/src/features/ai-chat/model/chatTypes.ts
  - frontend/src/features/ai-chat/hooks/useChatSession.test.tsx
autonomous: true
requirements: [AIMEM-01, AIMEM-03]
must_haves:
  truths:
    - "First-party chat sends turn updates as prompt-based requests tied to a stable chat session ID instead of replaying full message history each request."
    - "Reset clears server-side session memory for the same session ID and preserves deterministic local request lifecycle behavior."
    - "Latest-turn input is always sent as the primary request signal, preventing older client replay payloads from dominating server recommendation intent."
  artifacts:
    - path: "frontend/src/hooks/useAi.ts"
      provides: "Frontend AI request type contract that supports prompt + sessionId and reset + sessionId payloads."
      exports: ["AiCourseRecommendationsRequest", "useAiCourseRecommendations"]
    - path: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      provides: "Chat-session hook with stable per-tab session ID generation and prompt-first mutation payload behavior."
      contains: "sessionId"
    - path: "frontend/src/features/ai-chat/model/chatTypes.ts"
      provides: "Retry payload metadata aligned with prompt/session request semantics."
      contains: "ChatFailedPromptPayload"
    - path: "frontend/src/features/ai-chat/hooks/useChatSession.test.tsx"
      provides: "Hook-level regressions for prompt payload shape, retry behavior, and reset session-key parity."
      contains: "describe(\"useChatSession\")"
  key_links:
    - from: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      to: "frontend/src/hooks/useAi.ts"
      via: "send/retry/reset mutations pass prompt/reset bodies with stable sessionId to /ai/course-recommendations"
      pattern: "aiRec\.mutate\\("
    - from: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      to: "api/src/routes/ai.ts"
      via: "request shape aligns with route session-aware prompt contract introduced in plan 08-02"
      pattern: "sessionId"
    - from: "frontend/src/features/ai-chat/hooks/useChatSession.test.tsx"
      to: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      via: "tests assert payloads exclude full messages and include stable session IDs across send/retry/reset"
      pattern: "expect\\(mutate\\)"
---

<objective>
Align first-party chat client payloads with server session-memory contracts so route-level memory reliability is actually exercised in production usage.

Purpose: Close the Phase 8 client-side gap where sending full `messages` payloads bypasses server memory orchestration and weakens latest-turn priority.
Output: Prompt/sessionId-first frontend request flow with hook-level regression tests.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-memory-and-multi-turn-context-reliability/08-RESEARCH.md
@.planning/phases/08-memory-and-multi-turn-context-reliability/08-02-PLAN.md
@frontend/src/hooks/useAi.ts
@frontend/src/features/ai-chat/hooks/useChatSession.ts
@frontend/src/features/ai-chat/model/chatTypes.ts
@frontend/src/pages/AiChat.interactions.test.tsx

<interfaces>
Current frontend AI request union:
```typescript
export type AiCourseRecommendationsRequest =
  | { prompt: string; reset?: boolean; excludeCourseIds?: number[]; filters?: AiRecommendationFilters; preferences?: AiPreferenceSignals }
  | { messages: AiMessage[]; reset?: boolean; excludeCourseIds?: number[]; filters?: AiRecommendationFilters; preferences?: AiPreferenceSignals }
  | { reset: true };
```

Current `useChatSession` mutation payload path:
```typescript
aiRec.mutate({
  messages: nextMessages,
  preferences,
});
```

Retry payload shape:
```typescript
type ChatFailedPromptPayload = {
  prompt: string;
  messages: { role: "user" | "assistant"; content: string }[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend AI request contract for prompt/session and reset/session payloads</name>
  <files>frontend/src/hooks/useAi.ts, frontend/src/features/ai-chat/model/chatTypes.ts</files>
  <action>Update request typing to support `sessionId` for prompt and reset payload variants while preserving compatibility with existing optional filter/preference fields. Keep `messages` variant available for non-first-party callers, but define first-party hook payload contracts around prompt/session semantics. Adjust failed-prompt metadata types if needed so retry logic can preserve prompt and session identity without requiring full message replay.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Frontend request types can represent server-aligned prompt/session operations without breaking existing API call sites.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor useChatSession to generate stable session IDs and send prompt-first mutation payloads</name>
  <files>frontend/src/features/ai-chat/hooks/useChatSession.ts</files>
  <action>Introduce a stable per-tab chat session identifier (persisted in `sessionStorage` for the tab lifecycle) and include it in all send/retry/reset mutations. Switch first-party sends from `messages` replay to `{ prompt, sessionId, preferences }`, while keeping local `turns`/`aiMessages` state for rendering and retry UX only. Ensure reset sends `{ reset: true, sessionId }` after local state clear so server memory clearing remains scoped to the same chat session.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>First-party chat requests consistently target the same server session and no longer bypass server memory behavior through full-history message replay.</done>
</task>

<task type="auto">
  <name>Task 3: Add hook-level regressions for send/retry/reset payload shape and session ID stability</name>
  <files>frontend/src/features/ai-chat/hooks/useChatSession.test.tsx</files>
  <action>Create Vitest hook/component-harness tests that mock `useAiCourseRecommendations` and assert: send uses `{ prompt, sessionId }` (not `messages`); retries preserve original prompt with the same session ID; reset issues `{ reset: true, sessionId }`; and deep-link sends also include the same stable session ID. Include one assertion that session ID remains stable across multiple sends within the same hook instance.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/features/ai-chat/hooks/useChatSession.test.tsx</automated>
  </verify>
  <done>Frontend regressions lock prompt/session request behavior and prevent accidental reintroduction of full-history payload replay in first-party chat.</done>
</task>

</tasks>

<verification>
- `pnpm --filter frontend build` passes after request contract and hook refactor.
- `pnpm --filter frontend test -- src/features/ai-chat/hooks/useChatSession.test.tsx` passes for payload-shape and session-ID stability checks.
- Existing chat interaction tests continue to pass: `pnpm --filter frontend test -- src/pages/AiChat.interactions.test.tsx`.
</verification>

<success_criteria>
- First-party chat sends prompt-first, session-scoped requests that exercise server memory guarantees.
- Reset uses the same session ID channel as send/retry, ensuring scoped memory clearing.
- Latest-turn prompt is always the primary client signal into recommendation generation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-memory-and-multi-turn-context-reliability/08-memory-and-multi-turn-context-reliability-03-SUMMARY.md`
</output>
