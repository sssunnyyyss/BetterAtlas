---
phase: 06-atlas-grounding-and-recommendation-safety
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - api/src/ai/grounding/filterConstraintGuard.ts
  - api/src/routes/ai.ts
  - api/src/routes/ai.grounding-safety.test.ts
autonomous: true
requirements: [AIGRD-04]
must_haves:
  truths:
    - "Returned recommendations obey active catalog filters as hard constraints even if upstream retrieval/regression drift occurs."
    - "Grounding/filter policy violations trigger a safe fallback response path instead of leaking unsafe recommendations."
    - "Route-level regression tests prove grounding safety behavior for off-catalog mentions, blocked courses, and filter constraints."
  artifacts:
    - path: "api/src/ai/grounding/filterConstraintGuard.ts"
      provides: "Deterministic filter-satisfaction predicates and recommendation output guard utilities."
      exports: ["courseSatisfiesAiFilters", "enforceRecommendationFilterConstraints"]
    - path: "api/src/routes/ai.ts"
      provides: "Final recommendation safety gate (grounding + filter guard + safe fallback) before response return, with non-production diagnostics."
      contains: "safeFallbackUsed"
    - path: "api/src/routes/ai.grounding-safety.test.ts"
      provides: "Route-level safety regression suite covering grounding failures, blocklist enforcement, and hard filter constraints."
      contains: "describe(\"POST /ai/course-recommendations grounding safety\")"
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/grounding/filterConstraintGuard.ts"
      via: "final recommendation list is validated against activeFilters immediately before res.json return"
      pattern: "enforceRecommendationFilterConstraints\\("
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/grounding/safeGroundingFallback.ts"
      via: "grounding/filter guard failures route to deterministic safe fallback response"
      pattern: "buildSafeGroundingFallback\\("
    - from: "api/src/routes/ai.grounding-safety.test.ts"
      to: "api/src/routes/ai.ts"
      via: "tests assert no unsafe recommendation/mention leakage under modeled failure cases"
      pattern: "safe fallback"
---

<objective>
Enforce hard filter constraints at output time and lock Atlas grounding safety behavior with route-level regression gates.

Purpose: Satisfy AIGRD-04 and complete phase-level safety enforcement by proving fail-closed behavior under grounding/filter violations.
Output: Filter guard helper, route safety-gate wiring, and comprehensive route regression tests.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-atlas-grounding-and-recommendation-safety/06-RESEARCH.md
@.planning/phases/06-atlas-grounding-and-recommendation-safety/06-02-PLAN.md
@api/src/routes/ai.ts
@api/src/services/courseService.ts
@api/src/routes/ai.intent-routing.test.ts

<interfaces>
Active filter shape from route:
```typescript
type AiCourseFilters = {
  semester?: string;
  department?: string;
  minRating?: number;
  credits?: number;
  attributes?: string;
  instructor?: string;
  campus?: string;
  componentType?: string;
  instructionMethod?: string;
};
```

Recommendation payload item shape used in route:
```typescript
{
  course: CourseWithRatings;
  fitScore: number;
  why: string[];
  cautions: string[];
}
```

Guard contracts to add:
```typescript
export function courseSatisfiesAiFilters(course: CourseWithRatings, filters: AiCourseFilters): boolean;
export function enforceRecommendationFilterConstraints<T extends { course: CourseWithRatings }>(
  recommendations: T[],
  filters: AiCourseFilters
): { valid: T[]; droppedCount: number };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic recommendation filter-constraint guard utilities</name>
  <files>api/src/ai/grounding/filterConstraintGuard.ts</files>
  <action>Create pure predicates that evaluate each returned recommendation course against active filter constraints (semester, department, minRating, credits, attributes/GER, instructor, campus, component type, instruction method). Keep matching semantics aligned with route-normalized filters and return structured counts of dropped recommendations for diagnostics.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>A reusable guard can deterministically remove recommendation items that violate active filters and expose drop counts.</done>
</task>

<task type="auto">
  <name>Task 2: Add final safety gate in route with fail-closed fallback + debug diagnostics</name>
  <files>api/src/routes/ai.ts</files>
  <action>Before final `res.json`, run grounding validation (from Plan 06-01), then apply filter-constraint guard to assembled recommendations. If grounding fails or filtering drops all recommendations in a safety-violation scenario, return deterministic safe fallback response with empty recommendations. Extend non-production debug payload with `groundingStatus`, `groundingViolationCount`, `excludedMentionCount`, and `safeFallbackUsed` while preserving production response compatibility.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Route returns only safety-compliant recommendations, and violation paths fail closed with explicit debug observability outside production.</done>
</task>

<task type="auto">
  <name>Task 3: Create route-level grounding safety regression suite</name>
  <files>api/src/routes/ai.grounding-safety.test.ts</files>
  <action>Add a dedicated route-style Vitest suite for `POST /ai/course-recommendations` that asserts: off-catalog explicit assistant mention triggers safe fallback with no recommendations; excluded/disliked course IDs never appear in recommendations across sequential same-user calls; active filters are enforced as hard constraints on output cards; and JSON-format fallback (`openAiChat`) path still runs through the same safety gate.</action>
  <verify>
    <automated>pnpm --filter api test -- src/routes/ai.grounding-safety.test.ts</automated>
  </verify>
  <done>Regression tests fail on grounding/filter safety leaks and pass when hard safety constraints are enforced end-to-end.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` passes after final safety-gate integration.
- `pnpm --filter api test -- src/routes/ai.grounding-safety.test.ts` passes for grounding and hard-filter safety assertions.
- Route diagnostics confirm safety gate outcomes in non-production without changing production contract.
</verification>

<success_criteria>
- Output recommendation cards always satisfy active catalog filters as hard constraints.
- Any grounding/filter safety violation produces safe fallback behavior instead of unsafe output leakage.
- Route-level automated tests lock Atlas grounding and recommendation safety behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/06-atlas-grounding-and-recommendation-safety/06-atlas-grounding-and-recommendation-safety-03-SUMMARY.md`
</output>
