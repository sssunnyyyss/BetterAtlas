---
phase: 06-atlas-grounding-and-recommendation-safety
plan: 04
type: execute
wave: 4
depends_on: ["01", "03"]
files_modified:
  - api/src/ai/grounding/groundingValidator.ts
  - api/src/ai/grounding/groundingValidator.test.ts
  - api/src/ai/grounding/filterConstraintGuard.ts
  - api/src/routes/ai.grounding-safety.test.ts
autonomous: true
gap_closure: true
requirements: [AIGRD-01, AIGRD-04]
must_haves:
  truths:
    - "Fabricated title-only specific course mentions in assistant text fail grounding validation and return safe fallback."
    - "Returned recommendations fail closed when any active hard filter cannot be verified from recommendation metadata."
    - "Route regression coverage proves hard-filter enforcement across semester, credits, attributes, instructor, campus, component type, and instruction method."
  artifacts:
    - path: "api/src/ai/grounding/groundingValidator.ts"
      provides: "Deterministic unknown title-like mention detection in addition to code mention validation."
      exports: ["validateAssistantGrounding"]
    - path: "api/src/ai/grounding/filterConstraintGuard.ts"
      provides: "Fail-closed hard-filter predicates when required metadata is missing from recommendation payloads."
      exports: ["courseSatisfiesAiFilters", "enforceRecommendationFilterConstraints"]
    - path: "api/src/routes/ai.grounding-safety.test.ts"
      provides: "Route-level proof that title-only hallucinations and missing-metadata filter mismatches trigger safe outcomes."
      contains: "describe(\"POST /ai/course-recommendations grounding safety\")"
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/grounding/groundingValidator.ts"
      via: "validateAssistantGrounding(...) rejects unknown title-only mentions before response return"
      pattern: "validateAssistantGrounding\\("
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/grounding/filterConstraintGuard.ts"
      via: "enforceRecommendationFilterConstraints(...) removes non-verifiable filter-mismatch cards before final payload"
      pattern: "enforceRecommendationFilterConstraints\\("
    - from: "api/src/routes/ai.grounding-safety.test.ts"
      to: "api/src/routes/ai.ts"
      via: "in-process route execution asserts fallback and hard-filter behavior end-to-end"
      pattern: "postRecommendation\\("
---

<objective>
Close the two explicit verification failures for grounding strictness and hard filter fail-closed behavior.

Purpose: Complete Phase 6 safety guarantees by eliminating title-only grounding blind spots and metadata pass-through loopholes in output filtering.
Output: Updated grounding/filter guards with regression coverage that proves both gap truths now pass.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-atlas-grounding-and-recommendation-safety/06-VERIFICATION.md
@.planning/phases/06-atlas-grounding-and-recommendation-safety/06-atlas-grounding-and-recommendation-safety-01-SUMMARY.md
@.planning/phases/06-atlas-grounding-and-recommendation-safety/06-atlas-grounding-and-recommendation-safety-03-SUMMARY.md
@api/src/ai/grounding/groundingValidator.ts
@api/src/ai/grounding/filterConstraintGuard.ts
@api/src/routes/ai.grounding-safety.test.ts

<interfaces>
From api/src/ai/grounding/groundingValidator.ts:
```typescript
export function validateAssistantGrounding(
  input: GroundingValidationInput
): GroundingValidationResult;
```

From api/src/ai/grounding/filterConstraintGuard.ts:
```typescript
export function courseSatisfiesAiFilters(course: CourseWithRatings, filters: AiCourseFilters): boolean;
export function enforceRecommendationFilterConstraints<T extends { course: CourseWithRatings }>(
  recommendations: T[],
  filters: AiCourseFilters
): { valid: T[]; droppedCount: number };
```

From api/src/routes/ai.ts fail-closed safety behavior:
```typescript
if (!groundingResult.ok) return res.json(buildSafeGroundingFallback());
if (filterConstraintDroppedCount > 0 && recommendations.length === 0) {
  return res.json(buildSafeGroundingFallback());
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unknown title-only mention detection to grounding validator</name>
  <files>api/src/ai/grounding/groundingValidator.ts, api/src/ai/grounding/groundingValidator.test.ts</files>
  <action>Extend grounding validation so explicit title-like course mentions that do not map to active candidate titles are classified as `unknown_mention` (not silently accepted). Keep detection deterministic and bounded: treat clearly course-like title spans as violations while avoiding generic conversational phrases. Add unit tests that prove fabricated title-only mentions fail closed, valid candidate titles still pass, and deterministic output remains stable.</action>
  <verify>
    <automated>pnpm --filter api test -- src/ai/grounding/groundingValidator.test.ts</automated>
  </verify>
  <done>Grounding validator rejects fabricated title-only references and unit coverage locks the new behavior without regressing valid title matches.</done>
</task>

<task type="auto">
  <name>Task 2: Make output filter guard fail closed when active filters cannot be verified</name>
  <files>api/src/ai/grounding/filterConstraintGuard.ts</files>
  <action>Update hard-filter predicates so when an active filter requires course metadata (semester, component type, instruction method), missing metadata is treated as non-compliant and the recommendation is dropped. Preserve strict behavior for credits/attributes/instructor/campus and keep matching semantics aligned with normalized route filters. Do not keep permissive pass-through branches for active hard constraints.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Any recommendation lacking required metadata for an active hard filter is filtered out before response return.</done>
</task>

<task type="auto">
  <name>Task 3: Expand route grounding safety tests for title-only hallucinations and full hard-filter matrix</name>
  <files>api/src/routes/ai.grounding-safety.test.ts</files>
  <action>Add route-level regression scenarios that assert: (1) fabricated title-only assistant mentions trigger safe grounding fallback with empty recommendations; and (2) active semester/credits/attributes/instructor/campus/component/instruction-method filters are enforced as hard output constraints, including cases where recommendation payloads are missing required metadata. Keep tests in-process and deterministic using existing route test harness/mocks.</action>
  <verify>
    <automated>pnpm --filter api test -- src/routes/ai.grounding-safety.test.ts</automated>
  </verify>
  <done>Route tests fail on both verification-gap behaviors and pass when title-only grounding failures and fail-closed hard-filter enforcement are correctly implemented.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api test -- src/ai/grounding/groundingValidator.test.ts` passes with new title-only unknown mention coverage.
- `pnpm --filter api test -- src/routes/ai.grounding-safety.test.ts` passes with title-only fallback and hard-filter missing-metadata cases.
- `pnpm --filter api build` passes after filter guard changes.
</verification>

<success_criteria>
- Phase verification truth for AIGRD-01 passes for both unknown code and unknown title-only specific mentions.
- Phase verification truth for AIGRD-04 passes with fail-closed enforcement when active filters cannot be verified from recommendation payload metadata.
- Gap report in `06-VERIFICATION.md` can be re-run with no remaining failed truths.
</success_criteria>

<output>
After completion, create `.planning/phases/06-atlas-grounding-and-recommendation-safety/06-atlas-grounding-and-recommendation-safety-04-SUMMARY.md`
</output>
