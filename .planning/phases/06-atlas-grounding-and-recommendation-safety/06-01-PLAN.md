---
phase: 06-atlas-grounding-and-recommendation-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/ai/grounding/groundingContracts.ts
  - api/src/ai/grounding/groundingValidator.ts
  - api/src/ai/grounding/safeGroundingFallback.ts
  - api/src/ai/grounding/groundingValidator.test.ts
autonomous: true
requirements: [AIGRD-01, AIGRD-03]
must_haves:
  truths:
    - "Specific course mentions in assistant text are accepted only when they map to the active candidate set."
    - "Grounding violations fail closed to deterministic safe fallback text that contains no specific catalog course names."
    - "Grounding validation behavior is deterministic for repeated equivalent assistant outputs."
  artifacts:
    - path: "api/src/ai/grounding/groundingContracts.ts"
      provides: "Typed grounding interfaces for candidate indexing, mention extraction results, and violation reporting."
      exports: ["GroundingValidationInput", "GroundingValidationResult", "GroundingViolation"]
    - path: "api/src/ai/grounding/groundingValidator.ts"
      provides: "Pure validator that checks assistant course mentions against active candidates and blocked IDs."
      exports: ["validateAssistantGrounding"]
    - path: "api/src/ai/grounding/safeGroundingFallback.ts"
      provides: "Deterministic fail-closed fallback builder with no specific course entities."
      exports: ["buildSafeGroundingFallback"]
    - path: "api/src/ai/grounding/groundingValidator.test.ts"
      provides: "Unit regression matrix for pass/fail grounding cases and fallback-trigger conditions."
      contains: "describe(\"validateAssistantGrounding\")"
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/grounding/groundingValidator.ts"
      via: "recommend-mode orchestration validates modelResult.assistant_message before final response return"
      pattern: "validateAssistantGrounding\\("
    - from: "api/src/ai/grounding/groundingValidator.ts"
      to: "api/src/ai/grounding/safeGroundingFallback.ts"
      via: "violation outcome feeds fail-closed fallback branch in route"
      pattern: "buildSafeGroundingFallback"
    - from: "api/src/ai/grounding/groundingValidator.test.ts"
      to: "api/src/ai/grounding/groundingValidator.ts"
      via: "tests assert unknown/excluded mention classification and deterministic outcomes"
      pattern: "expect\\(result\\.ok\\)"
---

<objective>
Establish deterministic grounding contracts and a fail-closed policy before route integration.

Purpose: Satisfy AIGRD-01 and AIGRD-03 by treating model-generated assistant text as untrusted until grounded against active candidates.
Output: Grounding validator and safe fallback modules with unit coverage.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-atlas-grounding-and-recommendation-safety/06-RESEARCH.md
@api/src/routes/ai.ts

<interfaces>
From api/src/routes/ai.ts current recommendation response contract:
```typescript
{
  assistantMessage: string;
  followUpQuestion: string | null;
  recommendations: Array<unknown>;
}
```

Model output shape already parsed in route:
```typescript
const modelResponseSchema = z.object({
  assistant_message: z.string().min(1).max(2500),
  follow_up_question: z.string().min(1).max(400).nullable().optional(),
});
```

New contracts to add in this plan:
```typescript
export type GroundingValidationInput = {
  assistantMessage: string;
  candidates: CourseWithRatings[];
  blockedCourseIds: Set<number>;
};

export type GroundingViolation = {
  kind: "unknown_mention" | "blocked_mention";
  text: string;
};

export type GroundingValidationResult = {
  ok: boolean;
  violations: GroundingViolation[];
  matchedCandidateIds: number[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create grounding validator contracts and mention-normalization primitives</name>
  <files>api/src/ai/grounding/groundingContracts.ts, api/src/ai/grounding/groundingValidator.ts</files>
  <action>Create typed grounding contracts and deterministic mention matching utilities for course-code variants (`CS170`, `CS 170`, `CS-170`) plus normalized title matching. Keep this module pure (no route/global state access) so identical inputs always produce identical violation sets and matched candidate IDs.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Validator inputs/outputs are strongly typed and mention extraction is deterministic across code/title mention forms.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fail-closed grounding result classification and safe fallback builder</name>
  <files>api/src/ai/grounding/groundingValidator.ts, api/src/ai/grounding/safeGroundingFallback.ts</files>
  <action>Implement `validateAssistantGrounding` to return `ok=false` when assistant text contains explicit course-like mentions not mapped to candidates or mapped to blocked IDs. Add `buildSafeGroundingFallback` that returns concise, non-specific assistant guidance and an empty recommendation list payload shape suitable for route return. Do not include any catalog code/title tokens in fallback text.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Grounding failures are represented explicitly and safe fallback output is deterministic and course-name-free.</done>
</task>

<task type="auto">
  <name>Task 3: Add validator unit tests for pass, unknown-mention fail, and blocked-mention fail paths</name>
  <files>api/src/ai/grounding/groundingValidator.test.ts</files>
  <action>Create Vitest coverage that proves: valid candidate mentions pass; fabricated/off-catalog mentions fail as `unknown_mention`; blocked candidate mentions fail as `blocked_mention`; and repeated identical input produces identical result objects. Include cases for code and title mention matching variants.</action>
  <verify>
    <automated>pnpm --filter api test -- src/ai/grounding/groundingValidator.test.ts</automated>
  </verify>
  <done>Grounding unit tests reliably fail on fabricated/excluded mention leakage and pass on valid grounded references.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` passes with new grounding modules.
- `pnpm --filter api test -- src/ai/grounding/groundingValidator.test.ts` passes for deterministic pass/fail grounding outcomes.
- Contracts are ready for route integration without altering external API response keys.
</verification>

<success_criteria>
- Assistant specific course mentions can be validated deterministically against active candidates.
- Grounding validation supports explicit fail-closed handling with safe text and no fabricated specifics.
- Unit coverage guards against regressions in mention matching and violation detection.
</success_criteria>

<output>
After completion, create `.planning/phases/06-atlas-grounding-and-recommendation-safety/06-atlas-grounding-and-recommendation-safety-01-SUMMARY.md`
</output>
