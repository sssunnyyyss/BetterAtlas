---
phase: 06-atlas-grounding-and-recommendation-safety
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - api/src/ai/grounding/sessionBlocklistState.ts
  - api/src/ai/grounding/sessionBlocklistState.test.ts
  - api/src/routes/ai.ts
autonomous: true
requirements: [AIGRD-02]
must_haves:
  truths:
    - "Excluded/disliked courses are blocked as hard constraints across recommendation stages, not only prompt guidance."
    - "For authenticated sessions, blocked-course state persists across turns within TTL so disliked/excluded courses do not reappear."
    - "Blocked courses are prevented in both recommendation cards and assistant mention-grounding acceptance."
  artifacts:
    - path: "api/src/ai/grounding/sessionBlocklistState.ts"
      provides: "User-scoped TTL blocklist helper for merging and reading blocked course IDs between turns."
      exports: ["getSessionBlockedCourseIds", "mergeSessionBlockedCourseIds", "clearSessionBlockedCourseIds"]
    - path: "api/src/routes/ai.ts"
      provides: "Recommendation branch integration that merges request excludes/dislikes with session blocklist before candidate assembly and grounding validation."
      contains: "excludeSet"
    - path: "api/src/ai/grounding/sessionBlocklistState.test.ts"
      provides: "Deterministic tests for TTL expiry, merge semantics, and per-user isolation."
      contains: "describe(\"sessionBlocklistState\")"
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/grounding/sessionBlocklistState.ts"
      via: "route composes blocked IDs from request + session state before retrieval and recommendation assembly"
      pattern: "mergeSessionBlockedCourseIds\\("
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/grounding/groundingValidator.ts"
      via: "blocked course IDs are passed into grounding validation to reject excluded mentions"
      pattern: "blockedCourseIds"
    - from: "api/src/ai/grounding/sessionBlocklistState.test.ts"
      to: "api/src/ai/grounding/sessionBlocklistState.ts"
      via: "tests assert TTL-bounded persistence and no cross-user leakage"
      pattern: "expect\\(getSessionBlockedCourseIds"
---

<objective>
Add deterministic session-level exclusion/dislike safety and wire it into recommendation orchestration.

Purpose: Satisfy AIGRD-02 so blocked courses are treated as hard policy constraints throughout recommendation generation and grounding checks.
Output: Session blocklist helper, route integration, and isolation/TTL regression tests.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-atlas-grounding-and-recommendation-safety/06-RESEARCH.md
@.planning/phases/06-atlas-grounding-and-recommendation-safety/06-01-PLAN.md
@api/src/routes/ai.ts

<interfaces>
From api/src/routes/ai.ts current exclusion inputs:
```typescript
excludeCourseIds?: number[];
preferences?: {
  liked?: PreferenceCourse[];
  disliked?: PreferenceCourse[];
};
```

Memory utility pattern already present in route:
```typescript
function getUserMemory(userId: string): AiMessage[];
function setUserMemory(userId: string, messages: AiMessage[]): void;
function clearUserMemory(userId: string): void;
```

Session blocklist contract to add:
```typescript
export function getSessionBlockedCourseIds(userId: string): Set<number>;
export function mergeSessionBlockedCourseIds(
  userId: string,
  blockedIds: Iterable<number>
): Set<number>;
export function clearSessionBlockedCourseIds(userId: string): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement user-scoped TTL blocklist state helper for excluded/disliked course IDs</name>
  <files>api/src/ai/grounding/sessionBlocklistState.ts</files>
  <action>Create a dedicated in-memory state helper (mirroring existing per-user memory TTL behavior) that stores blocked course IDs per user, enforces TTL expiry, dedupes IDs, and supports explicit clear/reset. Keep helper deterministic and side-effect scoped to this module so route integration remains orchestration-only.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Blocklist helper exposes get/merge/clear operations with bounded, per-user, TTL-controlled behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire merged blocklist into recommendation candidate assembly and grounding checks</name>
  <files>api/src/routes/ai.ts</files>
  <action>In recommend mode, merge blocked IDs from request `excludeCourseIds`, request `preferences.disliked`, and session blocklist (when `userId` exists). Use the merged set as the single source of truth across candidate filtering, mention-derived recommendations, fallback recommendation selection, and grounding validator input. Persist merged blocked IDs back to session state after processing so subsequent turns preserve exclusions/dislikes.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Recommendation flow uses one merged blocked-ID policy, and authenticated multi-turn sessions retain blocked courses consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic unit tests for blocklist merge, TTL expiry, and user isolation</name>
  <files>api/src/ai/grounding/sessionBlocklistState.test.ts</files>
  <action>Create Vitest tests that verify: merge operations dedupe IDs, blocklists persist within TTL then expire, and one user's blocked IDs never appear in another user's state. Include reset-clear behavior to support route reset parity.</action>
  <verify>
    <automated>pnpm --filter api test -- src/ai/grounding/sessionBlocklistState.test.ts</automated>
  </verify>
  <done>Session blocklist behavior is regression-locked for merge rules, TTL boundaries, and user isolation.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` passes after blocklist-state integration.
- `pnpm --filter api test -- src/ai/grounding/sessionBlocklistState.test.ts` passes for merge/TTL/isolation semantics.
- Recommend-mode orchestration enforces blocked IDs through candidate, mention, and fallback paths.
</verification>

<success_criteria>
- Excluded/disliked courses are hard-blocked across all recommendation assembly paths.
- Authenticated sessions preserve blocklists across turns within TTL and clear on reset.
- Blocked mention acceptance is aligned with grounding validation inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-atlas-grounding-and-recommendation-safety/06-atlas-grounding-and-recommendation-safety-02-SUMMARY.md`
</output>
