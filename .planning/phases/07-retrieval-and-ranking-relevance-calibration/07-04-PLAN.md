---
phase: 07-retrieval-and-ranking-relevance-calibration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/routes/ai.ts
  - api/src/routes/ai.relevance-calibration.test.ts
autonomous: true
gap_closure: true
requirements: [AIREL-01, AIREL-02, AIREL-03, AIREL-04]
must_haves:
  truths:
    - "Recommend-mode lexical retrieval always executes, even when derived `searchTerms` is empty."
    - "Retrieval telemetry remains deterministic (`lexical_only`, `hybrid`, `hybrid_degraded`) while lexical counts reflect an always-on lexical attempt."
    - "Route-level relevance calibration regressions continue to pass for bounded ranking, diversity guardrails, and low-relevance refine fallback behavior."
  artifacts:
    - path: "api/src/routes/ai.ts"
      provides: "Recommend-mode lexical retrieval fallback logic that guarantees a primary lexical search query when `searchTerms` is empty."
      contains: "searchCourses({"
    - path: "api/src/routes/ai.relevance-calibration.test.ts"
      provides: "Regression coverage for the empty-derived-terms recommend path proving lexical retrieval is not skipped."
      contains: "always executes lexical retrieval when derived search terms are empty"
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/services/courseService.ts"
      via: "recommend-mode primary lexical search call executes unconditionally using a fallback lexical query when needed"
      pattern: "searchCourses\\(\\{\\s*q:"
    - from: "api/src/routes/ai.relevance-calibration.test.ts"
      to: "api/src/routes/ai.ts"
      via: "route-level test exercises a recommend prompt that yields empty derived terms and asserts lexical search invocation + telemetry"
      pattern: "postRecommendation\\("
---

<objective>
Close the Phase 7 verification gap where recommend-mode lexical retrieval can be skipped when derived terms are empty.

Purpose: Restore AIREL-01 always-on lexical retrieval behavior without regressing existing Phase 7 ranking/diversity/sufficiency outcomes.
Output: Route fallback logic and a targeted regression test proving lexical retrieval always runs for recommend-mode prompts.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-retrieval-and-ranking-relevance-calibration/07-VERIFICATION.md
@.planning/phases/07-retrieval-and-ranking-relevance-calibration/07-retrieval-and-ranking-relevance-calibration-03-SUMMARY.md
@api/src/routes/ai.ts
@api/src/routes/ai.relevance-calibration.test.ts

<interfaces>
From `api/src/routes/ai.ts` (current gap site):
```typescript
const searchTerms = deriveAiSearchTerms(latestUser, deps).slice(0, 3);
let searchCoursesRaw: CourseWithRatings[] = [];
if (searchTerms.length > 0) {
  const primary = await searchCourses({ q: searchTerms.join(" "), page: 1, limit: 24, ...activeFilters });
  // ...
}
```

Gap closure contract:
```typescript
// Recommend mode must always perform a lexical catalog search.
// If searchTerms is empty, derive a fallback lexical query from normalized prompt text.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce unconditional recommend-mode lexical search with empty-term fallback query</name>
  <files>api/src/routes/ai.ts</files>
  <action>Refactor recommend-mode lexical retrieval so the primary `searchCourses` call always executes. Replace the `if (searchTerms.length > 0)` guard with a fallback lexical query path that uses `searchTerms.join(" ")` when available and otherwise a normalized prompt-derived query (for example `candidateQuery` or a trimmed prompt fallback). Keep the per-term broadening path conditional on `searchTerms.length > 1`. Do not change semantic attempt/degradation logic, bounded ranking behavior, diversity selection rules, or low-relevance sufficiency gating.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Recommend-mode requests no longer bypass lexical retrieval when derived terms are empty, and existing relevance pipeline behavior remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add route regression for empty-derived-term recommend prompts to prove lexical retrieval always runs</name>
  <files>api/src/routes/ai.relevance-calibration.test.ts</files>
  <action>Add a focused test case using a recommend prompt expected to produce empty/near-empty derived terms (for example wording dominated by stopwords). Assert `searchCourses` is still called in recommend mode, the response remains successful, and debug telemetry reflects a lexical attempt (`lexicalCount` and retrieval mode consistency). Keep existing AIREL-01..04 route scenarios passing in the same suite.</action>
  <verify>
    <automated>pnpm --filter api test -- src/routes/ai.relevance-calibration.test.ts</automated>
  </verify>
  <done>The regression suite fails if lexical retrieval is conditionally skipped for empty-derived-term recommend prompts.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api test -- src/routes/ai.relevance-calibration.test.ts` passes, including the new empty-term lexical-retrieval regression.
- `pnpm --filter api build` passes after route changes.
</verification>

<success_criteria>
- AIREL-01 gap is closed: recommend-mode lexical retrieval executes regardless of derived term emptiness.
- Retrieval telemetry remains coherent with always-on lexical behavior.
- Existing Phase 7 route-level coverage for AIREL-02, AIREL-03, and AIREL-04 remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-retrieval-and-ranking-relevance-calibration/07-retrieval-and-ranking-relevance-calibration-04-SUMMARY.md`
</output>
