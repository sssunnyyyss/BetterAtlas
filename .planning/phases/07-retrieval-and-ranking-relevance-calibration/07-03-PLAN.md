---
phase: 07-retrieval-and-ranking-relevance-calibration
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - api/src/routes/ai.ts
  - api/src/routes/ai.relevance-calibration.test.ts
  - api/src/ai/relevance/retrievalModePolicy.ts
  - api/src/ai/relevance/relevanceSufficiencyPolicy.ts
  - api/src/ai/relevance/rankingPolicy.ts
  - api/src/ai/relevance/diversityPolicy.ts
autonomous: true
requirements: [AIREL-01, AIREL-02, AIREL-03, AIREL-04]
must_haves:
  truths:
    - "Recommend-mode requests always execute lexical retrieval and conditionally semantic retrieval with explicit mode telemetry (`lexical_only`, `hybrid`, `hybrid_degraded`)."
    - "Returned recommendations are ranked via bounded preference/trainer signals and selected with diversity guardrails unless concentration is explicitly required."
    - "When relevance is insufficient, the route returns refinement guidance plus an empty recommendation list instead of forcing weak cards."
    - "Route-level automated regressions prove hybrid retrieval behavior, bounded ranking calibration, diversity policy, and low-relevance fallback behavior."
  artifacts:
    - path: "api/src/routes/ai.ts"
      provides: "Integrated recommendation pipeline using relevance policy modules with deterministic debug metadata and low-relevance refine fallback."
      contains: "retrievalMode"
    - path: "api/src/routes/ai.relevance-calibration.test.ts"
      provides: "Route-level regression suite for AIREL-01..04 behaviors under mocked retrieval/ranking conditions."
      contains: "describe(\"POST /ai/course-recommendations relevance calibration\")"
    - path: "api/src/ai/relevance/retrievalModePolicy.ts"
      provides: "Finalized retrieval envelope contracts consumed by route orchestration."
      exports: ["buildRetrievalEnvelope"]
    - path: "api/src/ai/relevance/rankingPolicy.ts"
      provides: "Finalized bounded ranking contract consumed by route orchestration."
      exports: ["rankCandidatesWithBoundedSignals"]
    - path: "api/src/ai/relevance/diversityPolicy.ts"
      provides: "Finalized diversity selector consumed before response recommendation cards are emitted."
      exports: ["selectWithDepartmentDiversity"]
    - path: "api/src/ai/relevance/relevanceSufficiencyPolicy.ts"
      provides: "Finalized sufficiency gate used to trigger refine guidance when recommendation quality is too low."
      exports: ["isRelevanceSufficient", "buildLowRelevanceRefineGuidance"]
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/relevance/retrievalModePolicy.ts"
      via: "route emits retrieval mode metadata and uses envelope counts to avoid implicit semantic behavior"
      pattern: "buildRetrievalEnvelope\\("
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/relevance/rankingPolicy.ts"
      via: "candidate ordering uses bounded ranking output before recommendation card selection"
      pattern: "rankCandidatesWithBoundedSignals\\("
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/relevance/diversityPolicy.ts"
      via: "final recommendation cards pass through diversity selector after ranking"
      pattern: "selectWithDepartmentDiversity\\("
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/relevance/relevanceSufficiencyPolicy.ts"
      via: "low-quality recommendation pools route to refine guidance with no cards"
      pattern: "isRelevanceSufficient\\("
    - from: "api/src/routes/ai.relevance-calibration.test.ts"
      to: "api/src/routes/ai.ts"
      via: "in-process route tests assert calibrated retrieval/ranking/fallback outcomes end-to-end"
      pattern: "postRecommendation\\("
---

<objective>
Integrate relevance calibration policies into the AI recommendation route and lock behavior with route-level regression tests.

Purpose: Complete AIREL-01..04 by replacing implicit heuristics with explicit policy wiring and verifiable end-to-end behavior.
Output: Route integration plus dedicated relevance calibration route test suite.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-retrieval-and-ranking-relevance-calibration/07-RESEARCH.md
@.planning/phases/07-retrieval-and-ranking-relevance-calibration/07-01-PLAN.md
@.planning/phases/07-retrieval-and-ranking-relevance-calibration/07-02-PLAN.md
@api/src/routes/ai.ts
@api/src/routes/ai.intent-routing.test.ts
@api/src/routes/ai.grounding-safety.test.ts

<interfaces>
Current route response shape:
```typescript
{
  assistantMessage: string;
  followUpQuestion: string | null;
  recommendations: Array<{
    course: CourseWithRatings;
    fitScore: number;
    why: string[];
    cautions: string[];
  }>;
}
```

Current non-production debug payload fields in route include:
```typescript
{
  searchTerms: string[];
  semanticUniqueCount: number;
  semanticCandidateCount: number;
  trainerBoostedCount: number;
  trainerDemotedCount: number;
  safeFallbackUsed: boolean;
}
```

Integration targets from wave-1 plans:
```typescript
buildRetrievalEnvelope(...)
rankCandidatesWithBoundedSignals(...)
selectWithDepartmentDiversity(...)
isRelevanceSufficient(...)
buildLowRelevanceRefineGuidance(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire explicit retrieval mode envelope into recommend-mode candidate assembly</name>
  <files>api/src/routes/ai.ts, api/src/ai/relevance/retrievalModePolicy.ts</files>
  <action>Replace implicit semantic bookkeeping in recommend mode with the retrieval envelope contract from Plan 07-01. Keep lexical retrieval mandatory, attempt semantic retrieval only when embeddings are available, and classify failures as `hybrid_degraded` without failing the request. Expose retrieval-mode diagnostics in non-production debug payloads (mode, semanticAttempted, semanticAvailable, lexicalCount, semanticCount) while preserving production response compatibility.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Route retrieval path deterministically records lexical/hybrid/degraded behavior and still returns safe results when semantic path fails.</done>
</task>

<task type="auto">
  <name>Task 2: Replace unbounded candidate scoring with bounded ranking + final diversity selector</name>
  <files>api/src/routes/ai.ts, api/src/ai/relevance/rankingPolicy.ts, api/src/ai/relevance/diversityPolicy.ts</files>
  <action>Integrate bounded ranking policy from Plan 07-02 so preference/trainer signals are clamped and base relevance remains primary. Apply department diversity selection at final recommendation-card assembly (not candidate pool only), with explicit concentration bypass when department filters or clear single-domain intent are present. Preserve deterministic ordering/tie-breakers and keep grounding/filter hard constraints from Phase 6 intact.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Recommendation cards reflect bounded score composition and final-output diversity behavior with concentration only when explicitly justified.</done>
</task>

<task type="auto">
  <name>Task 3: Add low-relevance refine fallback gate and route-level relevance calibration regressions</name>
  <files>api/src/routes/ai.ts, api/src/routes/ai.relevance-calibration.test.ts, api/src/ai/relevance/relevanceSufficiencyPolicy.ts</files>
  <action>Before emitting recommendation cards, evaluate sufficiency using Plan 07-01 policy helpers. When relevance is insufficient, return concise refinement guidance plus `recommendations: []` (distinct from grounding safety fallback). Add a dedicated route-style Vitest suite asserting: hybrid mode telemetry when semantic succeeds; degraded mode when semantic path fails; bounded ranking prevents trainer/preference over-amplification; diversity enforced in unconstrained prompts; concentration allowed with explicit department filters; and low-relevance scenarios return refine guidance with zero recommendations.</action>
  <verify>
    <automated>pnpm --filter api test -- src/routes/ai.relevance-calibration.test.ts</automated>
  </verify>
  <done>Route-level tests lock AIREL-01..04 with deterministic pass/fail coverage for retrieval, ranking, diversity, and low-relevance fallback behavior.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` passes after route relevance-policy integration.
- `pnpm --filter api test -- src/routes/ai.relevance-calibration.test.ts` passes for AIREL-01..04 regressions.
- Existing route suites remain green: `pnpm --filter api test -- src/routes/ai.intent-routing.test.ts src/routes/ai.grounding-safety.test.ts`.
</verification>

<success_criteria>
- Recommend mode exposes explicit retrieval mode semantics and never hides semantic degradation.
- Ranking remains relevance-led with bounded preference/trainer adjustments.
- Final recommendation lists are diversity-aware by default with explicit concentration exceptions.
- Low-relevance requests receive refinement guidance and empty cards instead of weak forced recommendations.
</success_criteria>

<output>
After completion, create `.planning/phases/07-retrieval-and-ranking-relevance-calibration/07-retrieval-and-ranking-relevance-calibration-03-SUMMARY.md`
</output>
