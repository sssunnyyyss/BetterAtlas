---
phase: 03-interaction-smoothness-mobile-composer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/features/ai-chat/model/chatTypes.ts
  - frontend/src/features/ai-chat/hooks/useChatSession.ts
  - frontend/src/features/ai-chat/components/ChatFeed.tsx
  - frontend/src/features/ai-chat/components/ChatRequestStatus.tsx
  - frontend/src/pages/AiChat.tsx
  - frontend/src/index.css
  - frontend/src/pages/AiChat.interactions.test.tsx
autonomous: true
requirements: [AIXP-02]
must_haves:
  truths:
    - "Prompt submission, waiting feedback, assistant render, and status settle follow a deterministic lifecycle instead of jitter-prone ad hoc transitions."
    - "Auto-scroll behavior is intent-aware and does not repeatedly smooth-scroll on every micro state change."
    - "Reduced-motion preference disables non-essential transition effects while preserving equivalent state feedback."
  artifacts:
    - path: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      provides: "Deterministic request lifecycle metadata and transition sequencing for chat interactions."
      exports: ["useChatSession"]
    - path: "frontend/src/features/ai-chat/components/ChatFeed.tsx"
      provides: "Controlled auto-scroll and response rendering choreography wired to lifecycle state."
      contains: "scrollIntoView"
    - path: "frontend/src/features/ai-chat/components/ChatRequestStatus.tsx"
      provides: "Lifecycle status rendering that aligns with deterministic send/success/error transitions."
      contains: "ChatRequestStatus"
    - path: "frontend/src/pages/AiChat.interactions.test.tsx"
      provides: "Regression coverage for deterministic transition order and reduced-motion fallback behavior."
      contains: "AIXP-02"
  key_links:
    - from: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      to: "frontend/src/features/ai-chat/components/ChatRequestStatus.tsx"
      via: "request lifecycle state and metadata props"
      pattern: "idle|sending|success|error"
    - from: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      to: "frontend/src/features/ai-chat/components/ChatFeed.tsx"
      via: "turn append timing and scroll intent signaling"
      pattern: "requestState|turns"
    - from: "frontend/src/index.css"
      to: "frontend/src/features/ai-chat/components/ChatFeed.tsx"
      via: "reduced-motion-aware transition class behavior"
      pattern: "prefers-reduced-motion"
---

<objective>
Make the send-to-response interaction lifecycle smooth and deterministic, with reduced-motion-safe transition choreography.

Purpose: Satisfy AIXP-02 by removing ambiguous transition timing and scroll jitter from the AI chat request lifecycle.
Output: Extended lifecycle contracts in session state, intent-aware feed/status choreography, and tests for transition determinism.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-interaction-smoothness-mobile-composer/03-RESEARCH.md
@.planning/phases/03-interaction-smoothness-mobile-composer/03-01-PLAN.md
@frontend/src/features/ai-chat/model/chatTypes.ts
@frontend/src/features/ai-chat/hooks/useChatSession.ts
@frontend/src/features/ai-chat/components/ChatFeed.tsx
@frontend/src/features/ai-chat/components/ChatRequestStatus.tsx
@frontend/src/index.css

<interfaces>
From frontend/src/features/ai-chat/model/chatTypes.ts:
```typescript
export type ChatRequestState = "idle" | "sending" | "success" | "error";
export type ChatTurn = UserChatTurn | AssistantChatTurn;
```

From frontend/src/features/ai-chat/hooks/useChatSession.ts:
```typescript
export type ChatSessionApi = {
  turns: ChatTurn[];
  requestState: ChatRequestState;
  isSending: boolean;
  sendPrompt: (prompt: string) => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend chat lifecycle contracts for deterministic transition sequencing</name>
  <files>frontend/src/features/ai-chat/model/chatTypes.ts, frontend/src/features/ai-chat/hooks/useChatSession.ts</files>
  <action>Add explicit lifecycle metadata in `useChatSession` (for example request token/sequence, last transition timestamp, and deterministic success-settle path back to idle) so state transitions cannot race. Keep existing `/api/ai/course-recommendations` request payload compatibility and retain current ChatShell decomposition from Phase 2.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Session state exposes deterministic transition semantics and no longer relies on implicit timing behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Apply intent-aware feed/status transition choreography with reduced-motion fallback</name>
  <files>frontend/src/features/ai-chat/components/ChatFeed.tsx, frontend/src/features/ai-chat/components/ChatRequestStatus.tsx, frontend/src/pages/AiChat.tsx, frontend/src/index.css</files>
  <action>Replace unconditional smooth-scroll and generic transition triggers with intent-aware behavior (auto-scroll when near-bottom or immediately after user send/retry, otherwise preserve reader position). Ensure animations/transitions honor `prefers-reduced-motion` by falling back to non-animated behavior while keeping status clarity intact.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/pages/AiChat.interactions.test.tsx -t \"deterministic lifecycle\"</automated>
  </verify>
  <done>Feed/status transitions are stable and reduced-motion users receive equivalent lifecycle feedback without disruptive animation.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic transition regression coverage</name>
  <files>frontend/src/pages/AiChat.interactions.test.tsx</files>
  <action>Expand `AiChat.interactions` tests to assert deterministic lifecycle ordering (`idle -> sending -> success/error -> idle`) and controlled scroll behavior. Add reduced-motion-mode assertions to confirm non-essential animation classes/behaviors are suppressed while request-state messaging remains visible and correct.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/pages/AiChat.interactions.test.tsx</automated>
  </verify>
  <done>Interaction tests reliably detect transition-order regressions and reduced-motion behavior regressions.</done>
</task>

</tasks>

<verification>
- `pnpm --filter frontend build` passes with updated lifecycle contracts.
- `pnpm --filter frontend test -- src/pages/AiChat.interactions.test.tsx` passes with deterministic lifecycle assertions.
- Reduced-motion path keeps lifecycle UX equivalent without non-essential motion.
</verification>

<success_criteria>
- Send/wait/response transitions follow deterministic sequencing and feel stable.
- Auto-scroll behavior avoids jitter and preserves user position unless send-intent requires bottom anchoring.
- Reduced-motion users experience the same request-state semantics with motion minimized.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interaction-smoothness-mobile-composer/03-interaction-smoothness-mobile-composer-02-SUMMARY.md`
</output>
