---
phase: 03-interaction-smoothness-mobile-composer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/ai-chat/hooks/useComposerViewport.ts
  - frontend/src/features/ai-chat/components/ChatShell.tsx
  - frontend/src/features/ai-chat/components/ChatComposer.tsx
  - frontend/src/pages/AiChat.tsx
  - frontend/src/test/utils/viewport.ts
  - frontend/src/pages/AiChat.interactions.test.tsx
autonomous: true
requirements: [AIXP-01]
must_haves:
  truths:
    - "The chat composer remains visible and reachable when the mobile keyboard reduces the visual viewport."
    - "Standalone and embedded chat both apply keyboard-safe spacing without breaking the Phase 2 shell structure."
    - "Keyboard-safe behavior is covered by automated viewport-driven interaction tests."
  artifacts:
    - path: "frontend/src/features/ai-chat/hooks/useComposerViewport.ts"
      provides: "VisualViewport-aware keyboard inset contract with safe fallbacks for unsupported browsers."
      exports: ["useComposerViewport"]
    - path: "frontend/src/pages/AiChat.tsx"
      provides: "Composer viewport wiring that keeps ChatShell decomposition unchanged."
      contains: "useComposerViewport"
    - path: "frontend/src/pages/AiChat.interactions.test.tsx"
      provides: "Interaction regression coverage for keyboard-safe composer behavior."
      contains: "describe(\"AiChat interactions\")"
    - path: "frontend/src/test/utils/viewport.ts"
      provides: "Test helper support for VisualViewport resize/offset simulation."
      exports: ["setViewportSize"]
  key_links:
    - from: "frontend/src/features/ai-chat/hooks/useComposerViewport.ts"
      to: "frontend/src/pages/AiChat.tsx"
      via: "hook output mapped to shell/composer bottom offset styling"
      pattern: "keyboardInset|viewportHeight"
    - from: "frontend/src/pages/AiChat.tsx"
      to: "frontend/src/features/ai-chat/components/ChatShell.tsx"
      via: "existing header/feed/composer slots with added viewport-safe container props"
      pattern: "<ChatShell"
    - from: "frontend/src/pages/AiChat.interactions.test.tsx"
      to: "frontend/src/test/utils/viewport.ts"
      via: "keyboard-open simulation assertions"
      pattern: "visualViewport|resize"
---

<objective>
Implement keyboard-safe composer ergonomics for mobile while preserving the Phase 2 ChatShell structure and API behavior.

Purpose: Satisfy AIXP-01 with a deterministic viewport-safe input contract that keeps prompt entry usable during on-screen keyboard interactions.
Output: A dedicated composer viewport hook, wired shell/composer spacing behavior, and interaction tests that simulate keyboard viewport changes.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-interaction-smoothness-mobile-composer/03-RESEARCH.md
@.planning/phases/02-chat-ui-foundation/02-chat-ui-foundation-03-SUMMARY.md
@frontend/src/pages/AiChat.tsx
@frontend/src/features/ai-chat/components/ChatShell.tsx
@frontend/src/features/ai-chat/components/ChatComposer.tsx
@frontend/src/test/utils/viewport.ts

<interfaces>
From frontend/src/pages/AiChat.tsx:
```typescript
type AiChatProps = {
  embedded?: boolean;
};
```

From frontend/src/features/ai-chat/components/ChatShell.tsx:
```typescript
export type ChatShellVariant = "standalone" | "embedded";
export function ChatShell(props: {
  variant?: ChatShellVariant;
  header: ReactNode;
  feed: ReactNode;
  composer: ReactNode;
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a VisualViewport-aware composer ergonomics hook and test helper support</name>
  <files>frontend/src/features/ai-chat/hooks/useComposerViewport.ts, frontend/src/test/utils/viewport.ts</files>
  <action>Create `useComposerViewport` that listens to `window.visualViewport` resize/scroll and computes a keyboard inset with graceful fallback when VisualViewport is unavailable. Extend viewport test utilities so interaction tests can emulate keyboard-open viewport reductions and offsets deterministically (no UA sniffing).</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/pages/AiChat.interactions.test.tsx</automated>
  </verify>
  <done>The hook exposes deterministic viewport metrics and test helpers can trigger keyboard-like viewport changes in jsdom tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire keyboard-safe inset behavior into AiChat shell/composer layout without changing decomposition</name>
  <files>frontend/src/pages/AiChat.tsx, frontend/src/features/ai-chat/components/ChatShell.tsx, frontend/src/features/ai-chat/components/ChatComposer.tsx</files>
  <action>Consume `useComposerViewport` in `AiChat` and apply the computed inset through shell/composer container styling (for example CSS variables or inline style) so the composer remains visible with the keyboard open. Preserve current ChatShell header/feed/composer composition and keep `/api/ai/course-recommendations` request usage unchanged.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Composer stays reachable under keyboard viewport shrink scenarios in both standalone and embedded chat modes while structural decomposition remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add interaction tests for keyboard-safe composer visibility and submit usability</name>
  <files>frontend/src/pages/AiChat.interactions.test.tsx</files>
  <action>Create `AiChat.interactions` tests that mock session state and assert keyboard-open viewport changes do not hide the composer zone, textarea, or send affordance. Cover at least one standalone and one embedded rendering case, and assert submit remains enabled/disabled only by draft/sending rules (not viewport state).</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/pages/AiChat.interactions.test.tsx</automated>
  </verify>
  <done>Automated tests fail on keyboard-related composer regressions and pass for the expected keyboard-safe interaction contract.</done>
</task>

</tasks>

<verification>
- `pnpm --filter frontend build` passes after keyboard-safe hook/layout wiring.
- `pnpm --filter frontend test -- src/pages/AiChat.interactions.test.tsx` passes with viewport/keyboard simulation coverage.
- ChatShell remains structurally decomposed as header/feed/composer with no API contract changes.
- Manual gate (required): On a real iOS Safari device (or iOS Simulator Safari), open `/ai` in standalone mode, focus the composer, and confirm keyboard-open state keeps textarea + send control visible without overlap; submit a prompt while keyboard is open.
- Manual gate (required): On a real Android Chrome device (or Android Emulator Chrome), repeat keyboard-open checks for `/ai` and embedded `/catalog` composer entry and confirm textarea + send control remain visible and tappable through open/close keyboard cycles.
</verification>

<success_criteria>
- Mobile keyboard-open scenarios keep composer input and send controls visible and usable.
- Standalone and embedded chat both honor the same keyboard-safe ergonomics contract.
- Automated interaction coverage protects the mobile composer behavior from regressions.
- iOS Safari and Android Chrome manual keyboard-open gates both pass before plan sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interaction-smoothness-mobile-composer/03-interaction-smoothness-mobile-composer-01-SUMMARY.md`
</output>
