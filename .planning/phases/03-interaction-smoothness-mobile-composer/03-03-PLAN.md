---
phase: 03-interaction-smoothness-mobile-composer
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - frontend/src/features/ai-chat/model/chatTypes.ts
  - frontend/src/features/ai-chat/hooks/useChatSession.ts
  - frontend/src/features/ai-chat/components/ChatFeed.tsx
  - frontend/src/features/ai-chat/components/ChatRequestStatus.tsx
  - frontend/src/pages/AiChat.tsx
  - frontend/src/pages/AiChat.interactions.test.tsx
autonomous: true
requirements: [AIXP-03, AIXP-04]
must_haves:
  truths:
    - "Failed AI requests surface a clear, visible retry action that reuses the last failed prompt without losing chat context."
    - "Error feedback is non-destructive: existing turns stay visible and draft text is preserved unless user resets chat."
    - "Starter prompt chips provide low-friction first-query onboarding and are visible only when conversation turns are empty (initial load and post-reset)."
  artifacts:
    - path: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      provides: "Retained failed-request payload plus retry command and deterministic starter-chip send behavior."
      exports: ["useChatSession"]
    - path: "frontend/src/features/ai-chat/components/ChatRequestStatus.tsx"
      provides: "Error-state messaging with explicit retry CTA wiring."
      contains: "Retry"
    - path: "frontend/src/features/ai-chat/components/ChatFeed.tsx"
      provides: "Starter chip rendering contract for the zero-turn onboarding state only (initial load and post-reset)."
      contains: "suggestionChips"
    - path: "frontend/src/pages/AiChat.interactions.test.tsx"
      provides: "Regression tests for retry and starter-chip interaction semantics."
      contains: "AIXP-03"
  key_links:
    - from: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      to: "frontend/src/features/ai-chat/components/ChatRequestStatus.tsx"
      via: "retry callback + error message props"
      pattern: "retry|error"
    - from: "frontend/src/pages/AiChat.tsx"
      to: "frontend/src/features/ai-chat/components/ChatFeed.tsx"
      via: "structured starter chip definitions and interaction handler wiring"
      pattern: "suggestionChips|onSuggestionSelect"
    - from: "frontend/src/features/ai-chat/components/ChatFeed.tsx"
      to: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      via: "chip-triggered prompt seed/send and preserved conversation state"
      pattern: "sendPrompt|draft"
---

<objective>
Deliver resilient recovery UX and starter-prompt onboarding so users can recover quickly from failures and begin conversations with minimal friction.

Purpose: Satisfy AIXP-03 and AIXP-04 through visible retry paths, non-destructive error handling, and a deterministic starter-chip visibility rule (show only when turn list is empty).
Output: Retry-aware session state and status UI, curated starter-chip interaction model with zero-turn-only visibility, and interaction tests for recovery/onboarding flows.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-interaction-smoothness-mobile-composer/03-RESEARCH.md
@.planning/phases/03-interaction-smoothness-mobile-composer/03-02-PLAN.md
@frontend/src/features/ai-chat/hooks/useChatSession.ts
@frontend/src/features/ai-chat/components/ChatFeed.tsx
@frontend/src/features/ai-chat/components/ChatRequestStatus.tsx
@frontend/src/pages/AiChat.tsx
@frontend/src/api/client.ts

<interfaces>
From frontend/src/api/client.ts:
```typescript
export class ApiError extends Error {
  constructor(public status: number, message: string);
}
```

From frontend/src/features/ai-chat/hooks/useChatSession.ts:
```typescript
export type ChatSessionApi = {
  draft: string;
  requestState: ChatRequestState;
  sendPrompt: (prompt: string) => void;
  resetChat: () => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add non-destructive error and retry state to chat session orchestration</name>
  <files>frontend/src/features/ai-chat/model/chatTypes.ts, frontend/src/features/ai-chat/hooks/useChatSession.ts</files>
  <action>Extend session state to retain last failed prompt payload and readable error message (prefer `ApiError.message`, fallback copy otherwise). Add a retry command that replays the exact failed payload, keeps existing turns/draft intact on failure, and returns lifecycle state to `sending` deterministically.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/pages/AiChat.interactions.test.tsx -t \"retry\"</automated>
  </verify>
  <done>Failure handling preserves conversation context and exposes deterministic retry orchestration without mutating API request shape.</done>
</task>

<task type="auto">
  <name>Task 2: Wire visible retry CTA and actionable error copy into request status UI</name>
  <files>frontend/src/features/ai-chat/components/ChatRequestStatus.tsx, frontend/src/pages/AiChat.tsx</files>
  <action>Update status UI to render explicit retry affordance only for error state, with clear accessible labeling and error detail text. Wire retry callback from `useChatSession` through `AiChat` into status component while preserving existing status semantics for idle/sending/success states.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Error state always presents a clear retry path and actionable message without breaking existing status rendering behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign starter chips as curated onboarding intents with deterministic behavior</name>
  <files>frontend/src/pages/AiChat.tsx, frontend/src/features/ai-chat/components/ChatFeed.tsx, frontend/src/pages/AiChat.interactions.test.tsx</files>
  <action>Replace static chip strings with structured starter intents (`id`, `label`, `prompt`, optional category) in deterministic order. Keep one consistent interaction rule across chips (click sends immediately through existing send path), and enforce one visibility rule: chips render only when `turns.length === 0` (initial load and post-reset), and are hidden for all non-empty conversations including error states.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/pages/AiChat.interactions.test.tsx</automated>
  </verify>
  <done>Starter chips provide a predictable first-query path with zero-turn-only visibility, and retry UX remains visible and functional after failures.</done>
</task>

</tasks>

<verification>
- `pnpm --filter frontend build` passes with retry/error/starter changes.
- `pnpm --filter frontend test -- src/pages/AiChat.interactions.test.tsx` passes with retry and starter-chip assertions.
- Failed requests keep conversation and draft context while exposing explicit retry.
- Interaction tests explicitly verify starter chips are shown only when `turns.length === 0` and hidden for non-empty conversations, including error states.
</verification>

<success_criteria>
- Users can recover from failed AI requests through a visible one-tap retry path.
- Error feedback does not erase conversation context or draft text.
- Starter prompts provide deterministic, low-friction onboarding in empty/reset states only (zero-turn rule), with chips hidden once a conversation has turns.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interaction-smoothness-mobile-composer/03-interaction-smoothness-mobile-composer-03-SUMMARY.md`
</output>
