---
phase: 04-recommendation-cards-quality-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/ai-chat/components/ChatAssistantBlock.tsx
  - frontend/src/features/ai-chat/components/RecommendationCard.tsx
  - frontend/src/features/ai-chat/components/RecommendationDisclosure.tsx
  - frontend/src/features/ai-chat/components/ChatAssistantBlock.test.tsx
  - frontend/src/index.css
autonomous: true
requirements: [AIRC-01, AIRC-02, AIRC-03, AIRC-04]
must_haves:
  truths:
    - "Each recommendation card exposes course code, title, and fit score in a scan-first summary row."
    - "Rationale is readable in concise bullets with optional progressive disclosure for overflow details."
    - "Every card has a clear primary action that navigates to the corresponding course detail page."
    - "Caution/constraint details are available on demand without overwhelming the default card hierarchy."
  artifacts:
    - path: "frontend/src/features/ai-chat/components/RecommendationCard.tsx"
      provides: "Dedicated recommendation-card primitive with summary, rationale, caution disclosure, and primary action."
      exports: ["RecommendationCard"]
    - path: "frontend/src/features/ai-chat/components/RecommendationDisclosure.tsx"
      provides: "Reusable disclosure control with accessible expanded/collapsed state semantics."
      exports: ["RecommendationDisclosure"]
    - path: "frontend/src/features/ai-chat/components/ChatAssistantBlock.tsx"
      provides: "Assistant-turn renderer that composes the redesigned recommendation card system."
      contains: "recommendations.map"
    - path: "frontend/src/features/ai-chat/components/ChatAssistantBlock.test.tsx"
      provides: "Automated coverage for scan hierarchy, rationale readability, caution disclosure, and primary CTA behavior."
      contains: "describe(\"ChatAssistantBlock recommendations\")"
  key_links:
    - from: "frontend/src/features/ai-chat/components/ChatAssistantBlock.tsx"
      to: "frontend/src/features/ai-chat/components/RecommendationCard.tsx"
      via: "assistant recommendation list maps each recommendation into RecommendationCard"
      pattern: "<RecommendationCard"
    - from: "frontend/src/features/ai-chat/components/RecommendationCard.tsx"
      to: "/catalog/:id"
      via: "primary CTA uses route link to course detail"
      pattern: "to={`/catalog/${course.id}`}"
    - from: "frontend/src/features/ai-chat/components/RecommendationCard.tsx"
      to: "frontend/src/features/ai-chat/components/RecommendationDisclosure.tsx"
      via: "secondary caution and overflow rationale content uses shared disclosure control"
      pattern: "<RecommendationDisclosure"
---

<objective>
Ship the redesigned recommendation card system with progressive disclosure so users can rapidly evaluate recommendations and reliably reach course details.

Purpose: Satisfy AIRC-01, AIRC-02, AIRC-03, and AIRC-04 without changing backend AI recommendation behavior.
Output: Modular recommendation card components, wired into assistant turns, with focused automated coverage.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-recommendation-cards-quality-hardening/04-RESEARCH.md
@frontend/src/features/ai-chat/components/ChatAssistantBlock.tsx
@frontend/src/hooks/useAi.ts

<interfaces>
From frontend/src/hooks/useAi.ts:
```typescript
export type AiCourseRecommendation = {
  course: CourseWithRatings;
  fitScore: number;
  why: string[];
  cautions: string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scan-first recommendation card primitives</name>
  <files>frontend/src/features/ai-chat/components/RecommendationCard.tsx, frontend/src/features/ai-chat/components/RecommendationDisclosure.tsx, frontend/src/index.css</files>
  <action>Create modular recommendation card primitives with explicit summary hierarchy: course code and title in a persistent top row, labeled fit score badge, concise rationale preview (first 2-3 bullets), and a dedicated primary CTA (`View course details`). Implement disclosure controls for overflow rationale and cautions using semantic buttons (`aria-expanded`, `aria-controls`) so secondary information stays available but visually subordinate by default.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/features/ai-chat/components/ChatAssistantBlock.test.tsx</automated>
  </verify>
  <done>Card primitives render key scan data first, expose secondary rationale/cautions through accessible progressive disclosure, and provide a clear route action per recommendation.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate redesigned cards into assistant-turn rendering</name>
  <files>frontend/src/features/ai-chat/components/ChatAssistantBlock.tsx</files>
  <action>Refactor `ChatAssistantBlock` to use the new card primitives for each recommendation and preserve assistant content/follow-up structure. Ensure recommendation rendering remains deterministic across repeated turns and keeps action/disclosure controls keyboard reachable. Avoid nested interactive element conflicts (do not wrap the entire card as a parent link when disclosures are present).</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Assistant turns display redesigned recommendation cards with clear scan hierarchy, readable rationale, accessible cautions, and reliable navigation affordance.</done>
</task>

<task type="auto">
  <name>Task 3: Add targeted regression tests for recommendation-card behavior</name>
  <files>frontend/src/features/ai-chat/components/ChatAssistantBlock.test.tsx</files>
  <action>Add component-focused tests that assert: (1) summary row includes code/title/fit score, (2) rationale preview is concise and can reveal overflow details, (3) caution content is hidden by default and expandable, and (4) primary CTA points to `/catalog/:id`. Include one case with empty cautions to verify disclosure controls are not rendered unnecessarily.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/features/ai-chat/components/ChatAssistantBlock.test.tsx</automated>
  </verify>
  <done>Automated tests fail on hierarchy/disclosure/action regressions and pass for the intended recommendation-card contract.</done>
</task>

</tasks>

<verification>
- `pnpm --filter frontend build` passes with the redesigned recommendation card components.
- `pnpm --filter frontend test -- src/features/ai-chat/components/ChatAssistantBlock.test.tsx` passes with coverage for AIRC-01..04 behaviors.
- Primary recommendation action navigates to `/catalog/:id` and remains obvious in standalone and embedded chat surfaces.
</verification>

<success_criteria>
- Recommendation cards prioritize scan-first data (code/title/fit) and keep secondary details progressive.
- Rationale and cautions are readable without overwhelming the default card layout.
- Primary course-detail action is explicit, keyboard reachable, and route-correct.
- Automated coverage protects the new card contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-recommendation-cards-quality-hardening/04-recommendation-cards-quality-hardening-01-SUMMARY.md`
</output>
