---
phase: 05-intent-routing-and-conversation-cadence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/ai/intent/intentRouter.ts
  - api/src/ai/intent/intentRouter.test.ts
autonomous: true
requirements: [AIINT-03]
must_haves:
  truths:
    - "Each user turn is deterministically classified as exactly one mode: `conversation`, `clarify`, or `recommend`."
    - "Equivalent prompt variants (case/punctuation/spacing differences) classify to the same intent mode."
    - "Intent routing logic is rule-first and does not depend on probabilistic or model-scored classification."
  artifacts:
    - path: "api/src/ai/intent/intentRouter.ts"
      provides: "Deterministic intent classifier contracts and ordered decision logic."
      exports: ["IntentMode", "IntentDecision", "classifyIntent"]
    - path: "api/src/ai/intent/intentRouter.test.ts"
      provides: "Determinism matrix that locks mode assignment for representative conversational, ambiguous, and explicit recommendation prompts."
      contains: "describe(\"classifyIntent\")"
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/intent/intentRouter.ts"
      via: "route imports and uses classifyIntent before selecting response cadence"
      pattern: "classifyIntent"
    - from: "api/src/ai/intent/intentRouter.test.ts"
      to: "api/src/ai/intent/intentRouter.ts"
      via: "test corpus asserts stable mode outputs for normalized prompt variants"
      pattern: "expect\\(classifyIntent"
---

<objective>
Define and lock deterministic user-turn intent routing contracts for Phase 5.

Purpose: Satisfy AIINT-03 by introducing an explicit 3-mode intent model that can consistently drive downstream retrieval and response behavior.
Output: A dedicated `intentRouter` module plus deterministic classifier tests.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-intent-routing-and-conversation-cadence/05-RESEARCH.md
@api/src/routes/ai.ts

<interfaces>
From api/src/routes/ai.ts:
```typescript
function isTrivialGreeting(text: string): boolean;
function isCourseSuggestionIntent(text: string): boolean;
```

Contract to add in this plan:
```typescript
export type IntentMode = "conversation" | "clarify" | "recommend";

export type IntentDecision = {
  mode: IntentMode;
  reason: string;
  signals: string[];
};

export function classifyIntent(input: {
  latestUser: string;
  recentMessages: { role: "user" | "assistant"; content: string }[];
}): IntentDecision;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create intent router contracts and ordered rule precedence</name>
  <files>api/src/ai/intent/intentRouter.ts</files>
  <action>Create a new intent-router module that exports `IntentMode`, `IntentDecision`, and `classifyIntent` with strict rule ordering: trivial greetings -> `conversation`; explicit recommendation requests (strong recommendation verbs and course-code signals) -> `recommend`; ambiguous course-seeking prompts lacking actionable constraints -> `clarify`; fallback -> `conversation`. Keep the classifier pure and deterministic (no LLM call, no random scoring) so repeated equivalent input always returns the same mode.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>`classifyIntent` exists with exhaustive `IntentMode` outputs and an implementation that deterministically follows the documented rule order.</done>
</task>

<task type="auto">
  <name>Task 2: Add normalization helpers that preserve deterministic intent outcomes</name>
  <files>api/src/ai/intent/intentRouter.ts</files>
  <action>Add normalization/signal helpers used by `classifyIntent` so casing, punctuation, and extra whitespace do not alter intent routing for equivalent prompts. Keep helper behavior explainable with explicit signal tags in the returned `IntentDecision.reason`/`signals` fields to support debug visibility and later regression assertions.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Classifier emits stable modes and structured reasons/signals for the same normalized user intent.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic classifier matrix tests</name>
  <files>api/src/ai/intent/intentRouter.test.ts</files>
  <action>Create a Vitest matrix covering representative prompts for `conversation`, `clarify`, and `recommend` modes, including normalization variants and boundary examples from phase research. Include assertions that each fixture maps to exactly one expected mode and that repeated invocations are stable. Do not rely on snapshot-only assertions; assert mode/reason values directly.</action>
  <verify>
    <automated>pnpm --filter api test -- src/ai/intent/intentRouter.test.ts</automated>
  </verify>
  <done>Automated tests fail on non-deterministic or incorrect mode classification and pass for the intended 3-mode contract.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` passes with the new intent-router contracts.
- `pnpm --filter api test -- src/ai/intent/intentRouter.test.ts` passes with deterministic mode assertions.
- The classifier contract is consumable by route orchestration without adding retrieval logic to classifier code.
</verification>

<success_criteria>
- Intent classification is explicitly modeled as `conversation` / `clarify` / `recommend`.
- Routing outputs are deterministic for repeated equivalent prompts.
- Test coverage protects the classifier from mode drift regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-intent-routing-and-conversation-cadence/05-intent-routing-and-conversation-cadence-01-SUMMARY.md`
</output>
