---
phase: 05-intent-routing-and-conversation-cadence
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - api/src/routes/ai.ts
  - api/src/routes/ai.intent-routing.test.ts
autonomous: true
requirements: [AIINT-04]
must_haves:
  truths:
    - "Trivial greetings receive a fast conversational reply without recommendation retrieval work."
    - "Greeting turns avoid unnecessary dependency/retrieval preparation before returning."
    - "Intent-routing cadence regressions are blocked by automated tests that include greeting behavior."
  artifacts:
    - path: "api/src/routes/ai.ts"
      provides: "Early greeting fast-path that exits before retrieval/dependency-heavy recommendation setup."
      contains: "isTrivialGreeting"
    - path: "api/src/routes/ai.intent-routing.test.ts"
      provides: "Route-level regression tests validating greeting fast-path and no-retrieval behavior."
      contains: "describe(\"POST /ai/course-recommendations intent routing\")"
  key_links:
    - from: "api/src/routes/ai.intent-routing.test.ts"
      to: "api/src/routes/ai.ts"
      via: "tests invoke route and assert greeting responses skip retrieval side effects"
      pattern: "trivial greeting"
    - from: "api/src/routes/ai.ts"
      to: "api/src/services/courseService.ts"
      via: "greeting branch returns before retrieval helpers are called"
      pattern: "return res\\.json\\("
    - from: "api/src/routes/ai.ts"
      to: "api/src/lib/openaiEmbeddings.ts"
      via: "greeting branch guarantees no embedding call path"
      pattern: "openAiEmbedText"
---

<objective>
Harden and regression-test trivial greeting handling so greeting turns remain fast and bypass recommendation retrieval overhead.

Purpose: Satisfy AIINT-04 with explicit fast-path execution and automated non-regression coverage.
Output: Route fast-path optimization plus intent-routing regression tests including greeting cases.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-intent-routing-and-conversation-cadence/05-RESEARCH.md
@.planning/phases/05-intent-routing-and-conversation-cadence/05-02-PLAN.md
@api/src/routes/ai.ts
@api/src/test/setup.ts

<interfaces>
From api/src/routes/ai.ts:
```typescript
router.post("/ai/course-recommendations", ...);
```

Expected greeting response shape:
```typescript
{
  assistantMessage: string;
  followUpQuestion: null;
  recommendations: [];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move trivial greeting handling to the earliest safe route branch</name>
  <files>api/src/routes/ai.ts</files>
  <action>Refactor the handler so trivial greeting detection happens before dependency-heavy recommendation setup work (department/trainer fetches, candidate retrieval prep, embeddings checks). Preserve existing memory semantics for authenticated users and maintain response contract compatibility. Keep greeting response deterministic and concise.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Greeting turns return immediately with conversational reply and skip unnecessary recommendation setup costs.</done>
</task>

<task type="auto">
  <name>Task 2: Add route-level intent-routing regression tests with greeting no-retrieval assertions</name>
  <files>api/src/routes/ai.intent-routing.test.ts</files>
  <action>Create tests for `POST /api/ai/course-recommendations` that mock retrieval/embedding/dependency calls and assert greeting inputs return the canned conversational response with `recommendations: []` while retrieval-related calls remain unused. Include at least one conversation, one clarify, and one recommend fixture in the same suite so greeting behavior is validated in the broader cadence matrix.</action>
  <verify>
    <automated>pnpm --filter api test -- src/routes/ai.intent-routing.test.ts</automated>
  </verify>
  <done>Automated tests fail if greeting requests trigger recommendation-retrieval paths or lose expected fast conversational output.</done>
</task>

<task type="auto">
  <name>Task 3: Wire targeted test command into phase verification cadence</name>
  <files>api/src/routes/ai.intent-routing.test.ts</files>
  <action>Ensure the new suite runs reliably under current Vitest include patterns and remains deterministic in CI. Keep mocks local to this file and avoid shared mutable global state across tests so repeated runs are stable.</action>
  <verify>
    <automated>pnpm --filter api test -- src/routes/ai.intent-routing.test.ts</automated>
  </verify>
  <done>Intent-routing regression suite executes consistently and can serve as the Phase 5 gate for greeting fast-path behavior.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` passes after greeting fast-path refactor.
- `pnpm --filter api test -- src/routes/ai.intent-routing.test.ts` passes with greeting no-retrieval assertions.
- Greeting responses remain contract-compatible and recommendation retrieval is skipped.
</verification>

<success_criteria>
- Trivial greeting turns return quickly with conversational text and no recommendations.
- Greeting turns do not perform unnecessary recommendation retrieval or embedding work.
- Automated regression coverage protects greeting fast-path behavior from future cadence changes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-intent-routing-and-conversation-cadence/05-intent-routing-and-conversation-cadence-03-SUMMARY.md`
</output>
