---
phase: 05-intent-routing-and-conversation-cadence
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - api/src/routes/ai.ts
  - api/src/ai/intent/intentRouter.ts
autonomous: true
requirements: [AIINT-01, AIINT-02]
must_haves:
  truths:
    - "Conversational turns no longer force recommendation retrieval or recommendation cards."
    - "Ambiguous recommendation asks return one concise clarifying question before any course list is returned."
    - "Recommendation retrieval/generation runs only when intent mode is `recommend`."
  artifacts:
    - path: "api/src/routes/ai.ts"
      provides: "Intent-mode-driven route branching (`conversation`, `clarify`, `recommend`) with retrieval restricted to recommend mode."
      contains: "decision.mode"
    - path: "api/src/routes/ai.ts"
      provides: "Clarify response path that returns assistant text + follow-up question with zero recommendations."
      contains: "followUpQuestion"
    - path: "api/src/ai/intent/intentRouter.ts"
      provides: "Classifier signal support for ambiguous recommendation detection used by route-level clarify branch."
      exports: ["classifyIntent"]
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/intent/intentRouter.ts"
      via: "route determines mode before any retrieval/candidate assembly branch"
      pattern: "classifyIntent\\("
    - from: "api/src/routes/ai.ts"
      to: "api/src/services/courseService.ts"
      via: "course retrieval functions are called only inside the recommend-mode branch"
      pattern: "if \\(decision\\.mode === \"recommend\"\\)"
    - from: "api/src/routes/ai.ts"
      to: "response body"
      via: "clarify branch returns recommendations: [] and non-null followUpQuestion"
      pattern: "recommendations: \\[]"
---

<objective>
Wire intent-mode cadence in the AI route so recommendation retrieval happens only when warranted and ambiguous asks are clarify-first.

Purpose: Satisfy AIINT-01 and AIINT-02 by turning classifier output into strict route behavior gates.
Output: Updated AI route orchestration with explicit `conversation`/`clarify`/`recommend` branching and retrieval gating.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-intent-routing-and-conversation-cadence/05-RESEARCH.md
@.planning/phases/05-intent-routing-and-conversation-cadence/05-01-PLAN.md
@api/src/routes/ai.ts
@api/src/lib/openai.ts

<interfaces>
From api/src/lib/openai.ts:
```typescript
export async function openAiChat(...): Promise<string>;
export async function openAiChatJson(...): Promise<{ raw: string; parsed: unknown }>;
```

From api/src/routes/ai.ts response contract:
```typescript
{
  assistantMessage: string;
  followUpQuestion: string | null;
  recommendations: Array<unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace binary suggestion gate with 3-mode intent cadence branching</name>
  <files>api/src/routes/ai.ts</files>
  <action>Integrate `classifyIntent` from Plan 05-01 and replace `wantsCourseSuggestions` branching with explicit `decision.mode` branching. Enforce behavior gates: `conversation` -> conversational response path with `recommendations: []`; `clarify` -> concise clarify question response with `recommendations: []`; `recommend` -> existing retrieval/ranking/recommendation path. Do not execute retrieval preparation (`searchCourses`, semantic retrieval, candidate assembly) unless mode is `recommend`.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Route flow is mode-driven and recommendation retrieval logic is unreachable from `conversation` and `clarify` branches.</done>
</task>

<task type="auto">
  <name>Task 2: Implement clarify-first ambiguous ask responses with stable output contract</name>
  <files>api/src/routes/ai.ts, api/src/ai/intent/intentRouter.ts</files>
  <action>Implement a concise clarify response generator used when mode is `clarify`. The response must include a direct `assistantMessage`, a non-null `followUpQuestion`, and `recommendations: []`; it must not include specific course suggestions in this turn. Keep clarification single-question and actionable (constraints like workload/GER/department/semester), and avoid routing this branch through recommendation candidate retrieval.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Ambiguous recommendation asks receive a short clarifying follow-up and return no course cards in the same response.</done>
</task>

<task type="auto">
  <name>Task 3: Add non-production intent-cadence diagnostics for verification</name>
  <files>api/src/routes/ai.ts</files>
  <action>Extend existing non-production debug payload to include intent routing metadata (`intentMode`, `intentReason`, `retrievalSkipped`) for each response branch. Keep diagnostics production-safe by preserving current production guard behavior. This metadata is required for deterministic verification and later observability alignment, but should not alter client response compatibility in production mode.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Debug output exposes enough signal to confirm retrieval gating and clarify-first behavior without changing production response shape.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` passes after mode-based branch refactor.
- Conversational turns and clarify turns both return `recommendations: []`.
- Clarify responses include concise follow-up text before any recommendation list.
- Retrieval/candidate work executes only when mode is `recommend`.
</verification>

<success_criteria>
- AI chat no longer forces recommendations on normal conversational turns.
- Ambiguous recommendation asks reliably produce clarify-first responses.
- Route-level control flow cleanly separates conversation/clarify from recommendation retrieval.
</success_criteria>

<output>
After completion, create `.planning/phases/05-intent-routing-and-conversation-cadence/05-intent-routing-and-conversation-cadence-02-SUMMARY.md`
</output>
