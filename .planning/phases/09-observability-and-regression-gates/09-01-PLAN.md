---
phase: 09-observability-and-regression-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/ai/observability/aiQualityTelemetry.ts
  - api/src/ai/observability/aiQualityTelemetry.test.ts
  - api/src/routes/ai.ts
  - api/src/routes/adminPrograms.ts
autonomous: true
requirements: [AIOPS-01]
must_haves:
  truths:
    - "Production requests emit a production-safe AI quality telemetry event for every terminal route outcome without logging prompt/session/course free-text."
    - "Telemetry snapshots expose intent mode, retrieval mode, fallback usage, and grounding mismatch indicators with low-cardinality counters."
    - "Admin monitoring can inspect AI quality telemetry using an authenticated admin-only endpoint surface."
  artifacts:
    - path: "api/src/ai/observability/aiQualityTelemetry.ts"
      provides: "Typed bounded-event recorder and in-memory aggregate snapshot API for AI quality monitoring."
      exports: ["recordAiQualityEvent", "getAiQualityTelemetrySnapshot", "resetAiQualityTelemetryForTests"]
    - path: "api/src/ai/observability/aiQualityTelemetry.test.ts"
      provides: "Unit regressions proving bounded key cardinality, mismatch/fallback aggregation, and snapshot stability."
      contains: "describe(\"aiQualityTelemetry\")"
    - path: "api/src/routes/ai.ts"
      provides: "Route-level instrumentation that emits one telemetry event per terminal response path."
      contains: "recordAiQualityEvent("
    - path: "api/src/routes/adminPrograms.ts"
      provides: "Admin-safe monitoring endpoint/snapshot exposure for AI quality telemetry aggregates."
      contains: "aiQualityTelemetry"
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/observability/aiQualityTelemetry.ts"
      via: "each terminal response path records a bounded telemetry event before returning JSON"
      pattern: "recordAiQualityEvent\\("
    - from: "api/src/routes/adminPrograms.ts"
      to: "api/src/ai/observability/aiQualityTelemetry.ts"
      via: "admin metrics response includes AI quality snapshot for production monitoring"
      pattern: "getAiQualityTelemetrySnapshot\\("
    - from: "api/src/ai/observability/aiQualityTelemetry.test.ts"
      to: "api/src/ai/observability/aiQualityTelemetry.ts"
      via: "tests validate expected counter buckets and reject drift in telemetry schema assumptions"
      pattern: "expect\\(snapshot\\."
---

<objective>
Add a production-safe telemetry foundation that captures AI quality outcomes for monitoring without exposing user content.

Purpose: Satisfy AIOPS-01 by creating deterministic, low-cardinality observability data that operations can inspect in production.
Output: New telemetry module, route instrumentation, and admin-accessible aggregate snapshot.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-observability-and-regression-gates/09-RESEARCH.md
@api/src/routes/ai.ts
@api/src/routes/adminPrograms.ts
@api/src/config/env.ts

<interfaces>
Current recommend route debug-relevant signals (already computed in `api/src/routes/ai.ts`):
```typescript
const retrievalDebug = {
  retrievalMode: retrievalEnvelope.mode,
  semanticAttempted: retrievalEnvelope.semanticAttempted,
  semanticAvailable: retrievalEnvelope.semanticAvailable,
  lexicalCount: retrievalEnvelope.lexicalCount,
  semanticCount: retrievalEnvelope.semanticCount,
};
```

Existing retrieval mode contract (`api/src/ai/relevance/retrievalModePolicy.ts`):
```typescript
export type RetrievalMode = "lexical_only" | "hybrid" | "hybrid_degraded";
```

Admin metrics surface pattern (`api/src/routes/adminPrograms.ts`):
```typescript
router.use(requireAuth, requireAdmin);
router.get("/system/metrics", async (_req, res) => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bounded AI quality telemetry module with aggregate snapshot APIs</name>
  <files>api/src/ai/observability/aiQualityTelemetry.ts, api/src/ai/observability/aiQualityTelemetry.test.ts</files>
  <action>Implement a typed telemetry recorder for AI route outcomes with low-cardinality dimensions only (intent mode, retrieval mode, outcome type, grounding status, fallback booleans, mismatch flag). Build in-memory aggregate counters and a snapshot accessor suitable for admin monitoring. Add unit tests that prove bounded bucketing behavior, mismatch/fallback accumulation, and deterministic snapshot structure.</action>
  <verify>
    <automated>pnpm --filter api test -- src/ai/observability/aiQualityTelemetry.test.ts</automated>
  </verify>
  <done>Telemetry storage and read APIs exist with regression coverage and no free-text/PII fields in event schema.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument POST /ai/course-recommendations terminal branches with one telemetry event per outcome</name>
  <files>api/src/routes/ai.ts</files>
  <action>Integrate telemetry calls into every terminal response path in the recommendation route (reset response, conversation/clarify skips, no-candidate outcome, grounding fallback, low-relevance refine, filter-constraint fallback, successful recommendation response, and route error catch path). Ensure outcome classification is deterministic and maps to bounded telemetry enums without leaking request content.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>All route outcomes produce exactly one production-safe telemetry event with consistent enum-based dimensions.</done>
</task>

<task type="auto">
  <name>Task 3: Expose AI quality telemetry snapshot through authenticated admin monitoring surface</name>
  <files>api/src/routes/adminPrograms.ts</files>
  <action>Extend the existing admin metrics surface to include an AI quality telemetry snapshot object (counts/rates for intent mode, retrieval mode, fallback usage, and grounding mismatch). Keep access admin-authenticated by reusing existing `requireAuth` and `requireAdmin` middleware protections.</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>Admin metrics consumers can retrieve aggregate AI quality telemetry without direct access to user prompts or catalog entities.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` passes with telemetry module and route instrumentation.
- `pnpm --filter api test -- src/ai/observability/aiQualityTelemetry.test.ts` passes for bounded schema and aggregate behavior.
- Admin metrics payload includes AI quality aggregate snapshot while remaining under admin auth middleware.
</verification>

<success_criteria>
- Production-safe telemetry now exists for intent/retrieval/fallback/grounding outcomes.
- Every recommend-route terminal branch contributes to monitoring counters.
- Operations has an authenticated surface to inspect AI quality aggregate health.
</success_criteria>

<output>
After completion, create `.planning/phases/09-observability-and-regression-gates/09-observability-and-regression-gates-01-SUMMARY.md`
</output>
