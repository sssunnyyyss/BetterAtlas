---
phase: 09-observability-and-regression-gates
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - api/src/routes/ai.observability.test.ts
  - api/package.json
  - package.json
  - docs/ai-regression-gates.md
autonomous: true
requirements: [AIOPS-02, AIOPS-01, AIOPS-03]
must_haves:
  truths:
    - "AI release gating is explicit and fails the build when intent/grounding/relevance/observability regressions occur."
    - "Automated observability regressions verify production-safe telemetry behavior and non-production diagnostics contract behavior."
    - "Engineers have one documented command path for pre-merge/pre-release AI safety verification."
  artifacts:
    - path: "api/src/routes/ai.observability.test.ts"
      provides: "Route-level regressions for telemetry event emission/snapshot behavior and debug contract gating by environment."
      contains: "describe(\"POST /ai/course-recommendations observability\")"
    - path: "api/package.json"
      provides: "Dedicated `test:ai:gates` script that runs build plus full AI regression matrix, including observability tests."
      contains: "test:ai:gates"
    - path: "package.json"
      provides: "Workspace-level command alias for release/CI invocation of the AI gate script."
      contains: "test:ai:gates"
    - path: "docs/ai-regression-gates.md"
      provides: "Runbook documenting gate scope, blocking policy, and expected debugging workflow when failures occur."
      contains: "AI Regression Gate"
  key_links:
    - from: "api/package.json"
      to: "api/src/routes/ai.intent-routing.test.ts"
      via: "gate script explicitly runs intent-routing, grounding-safety, relevance-calibration, memory-context, and observability suites"
      pattern: "src/routes/ai\\.(intent-routing|grounding-safety|relevance-calibration|memory-context|observability)\\.test\\.ts"
    - from: "api/src/routes/ai.observability.test.ts"
      to: "api/src/routes/ai.ts"
      via: "tests verify telemetry/debug contracts across route outcomes and environment gating behavior"
      pattern: "postRecommendation\\("
    - from: "docs/ai-regression-gates.md"
      to: "api/package.json"
      via: "runbook references canonical gate command and release-blocking policy"
      pattern: "pnpm --filter api run test:ai:gates"
---

<objective>
Package all AI behavioral and observability checks into a release-blocking regression gate with operational runbook guidance.

Purpose: Satisfy AIOPS-02 while reinforcing AIOPS-01 and AIOPS-03 through explicit observability contract tests and a single required gate command.
Output: Observability route tests, canonical gate scripts, and documented release policy.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-observability-and-regression-gates/09-RESEARCH.md
@.planning/phases/09-observability-and-regression-gates/09-01-PLAN.md
@.planning/phases/09-observability-and-regression-gates/09-02-PLAN.md
@api/src/routes/ai.ts
@api/src/routes/ai.intent-routing.test.ts
@api/src/routes/ai.grounding-safety.test.ts
@api/src/routes/ai.relevance-calibration.test.ts
@api/src/routes/ai.memory-context.test.ts
@api/package.json
@package.json

<interfaces>
Current API test script contract in `api/package.json`:
```json
{
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  }
}
```

Route test harness pattern used across existing AI suites:
```typescript
function getCourseRecommendationsHandlers() { ... }
async function postRecommendation(body: Record<string, unknown>) { ... }
```

Observability requirements to gate in this plan:
```text
AIOPS-01 -> telemetry visibility for intent/retrieval/fallback/grounding mismatch
AIOPS-02 -> automated release-blocking regression matrix
AIOPS-03 -> non-production diagnostics inspectability contract
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route-level observability regression suite for telemetry and diagnostics contracts</name>
  <files>api/src/routes/ai.observability.test.ts</files>
  <action>Create a dedicated route-style Vitest suite that validates: telemetry counters/events are emitted for key outcomes (recommend success, grounding fallback, low-relevance refine, reset, and route error); telemetry schema remains production-safe (no free-text prompt/course payload fields); and debug diagnostics are present only in non-production responses with expected contract keys.</action>
  <verify>
    <automated>pnpm --filter api test -- src/routes/ai.observability.test.ts</automated>
  </verify>
  <done>Observability behaviors are regression-locked and fail fast when telemetry/debug contracts drift.</done>
</task>

<task type="auto">
  <name>Task 2: Package release-blocking AI gate scripts at API and workspace levels</name>
  <files>api/package.json, package.json</files>
  <action>Add canonical gate scripts that run `api` build plus the full AI regression matrix: intent routing, grounding safety, relevance calibration, memory context, and observability tests. Provide a top-level alias so release workflows use one command path without manually listing suites.</action>
  <verify>
    <automated>pnpm --filter api run test:ai:gates</automated>
  </verify>
  <done>A single command enforces all AI behavior/observability release gates and returns non-zero on any regression.</done>
</task>

<task type="auto">
  <name>Task 3: Document AI regression gate policy and failure triage runbook</name>
  <files>docs/ai-regression-gates.md</files>
  <action>Write a concise runbook defining gate purpose, command usage, included suites, what qualifies as release-blocking failure, and triage expectations for telemetry/debug contract failures. Keep documentation aligned to the exact script names added in this plan.</action>
  <verify>
    <automated>rg -n \"test:ai:gates|ai.observability.test.ts|release-blocking\" docs/ai-regression-gates.md api/package.json package.json</automated>
  </verify>
  <done>Engineers have an authoritative, discoverable procedure for running and interpreting the AI regression gate before merge/release.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api test -- src/routes/ai.observability.test.ts` passes for telemetry/debug contract assertions.
- `pnpm --filter api run test:ai:gates` passes and includes all required AI suites.
- `pnpm run test:ai:gates` (workspace alias) resolves to the same release-blocking gate path.
</verification>

<success_criteria>
- AI safety and observability checks are consolidated into an explicit release gate.
- Observability regressions are covered by automated route tests, not manual inspection.
- Gate command and policy are documented for repeatable operational use.
</success_criteria>

<output>
After completion, create `.planning/phases/09-observability-and-regression-gates/09-observability-and-regression-gates-03-SUMMARY.md`
</output>
