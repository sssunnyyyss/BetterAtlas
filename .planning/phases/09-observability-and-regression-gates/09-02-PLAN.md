---
phase: 09-observability-and-regression-gates
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - api/src/ai/observability/aiDebugDiagnostics.ts
  - api/src/ai/observability/aiDebugDiagnostics.test.ts
  - api/src/routes/ai.ts
  - api/src/routes/ai.relevance-calibration.test.ts
autonomous: true
requirements: [AIOPS-03]
must_haves:
  truths:
    - "Non-production AI responses expose a consistent diagnostics contract across all recommend-route outcomes."
    - "Diagnostics include candidate composition, filter-enforcement evidence, and ranking-factor breakdowns for top candidates."
    - "Production responses do not expose debug diagnostics even when non-production contract expands."
  artifacts:
    - path: "api/src/ai/observability/aiDebugDiagnostics.ts"
      provides: "Centralized helper for assembling non-production debug payloads with shared/base + branch-specific fields."
      exports: ["buildAiDebugDiagnostics", "buildRankingTopBreakdown"]
    - path: "api/src/ai/observability/aiDebugDiagnostics.test.ts"
      provides: "Unit tests for diagnostics schema stability, ranking breakdown shape, and filter evidence field construction."
      contains: "describe(\"aiDebugDiagnostics\")"
    - path: "api/src/routes/ai.ts"
      provides: "Route debug payload assembly delegated to shared diagnostics builder to eliminate branch drift."
      contains: "buildAiDebugDiagnostics("
    - path: "api/src/routes/ai.relevance-calibration.test.ts"
      provides: "Route-level assertions covering new diagnostics fields for relevance/ranking/filter evidence."
      contains: "rankingTopBreakdown"
  key_links:
    - from: "api/src/routes/ai.ts"
      to: "api/src/ai/observability/aiDebugDiagnostics.ts"
      via: "all non-production debug payloads are generated through one helper contract"
      pattern: "buildAiDebugDiagnostics\\("
    - from: "api/src/ai/observability/aiDebugDiagnostics.ts"
      to: "api/src/ai/relevance/rankingPolicy.ts"
      via: "ranking breakdown maps ranked candidate score components into debug-safe response fields"
      pattern: "scores\\.baseRelevance|scores\\.preference|scores\\.trainer|scores\\.final"
    - from: "api/src/routes/ai.relevance-calibration.test.ts"
      to: "api/src/routes/ai.ts"
      via: "tests lock diagnostics payload keys and ranking/filter evidence to prevent schema regressions"
      pattern: "expect\\(body\\.debug\\."
---

<objective>
Harden non-production diagnostics into a stable, centralized contract with ranking and filter evidence visibility.

Purpose: Satisfy AIOPS-03 by making diagnostics inspectable and deterministic for QA/investigation without duplicative branch-local payload logic.
Output: Shared diagnostics builder, route refactor to use it, and regression checks on expanded debug schema.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-observability-and-regression-gates/09-RESEARCH.md
@.planning/phases/09-observability-and-regression-gates/09-01-PLAN.md
@api/src/routes/ai.ts
@api/src/ai/relevance/rankingPolicy.ts
@api/src/ai/grounding/filterConstraintGuard.ts
@api/src/config/env.ts
@api/src/routes/ai.relevance-calibration.test.ts

<interfaces>
Ranking score components available from `api/src/ai/relevance/rankingPolicy.ts`:
```typescript
export type RankedCandidate = {
  course: CourseWithRatings;
  index: number;
  scores: {
    baseRelevance: number;
    preference: number;
    trainer: number;
    final: number;
  };
};
```

Current non-production debug exposure pattern in `api/src/routes/ai.ts`:
```typescript
...(env.nodeEnv !== "production"
  ? { debug: { /* branch-local object literal */ } }
  : {})
```

Filter enforcement output contract in `api/src/ai/grounding/filterConstraintGuard.ts`:
```typescript
type EnforcedRecommendations = {
  valid: Recommendation[];
  droppedCount: number;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build centralized non-production diagnostics helper with ranking and filter evidence fields</name>
  <files>api/src/ai/observability/aiDebugDiagnostics.ts, api/src/ai/observability/aiDebugDiagnostics.test.ts</files>
  <action>Create a typed diagnostics builder that composes shared fields (intent/retrieval/timing/counts), branch-specific outcome flags, ranking-factor breakdown entries (baseRelevance/preference/trainer/final), and filter-enforcement evidence (dropped counts + whether constraint fallback triggered). Add focused unit tests for schema shape and deterministic field behavior.</action>
  <verify>
    <automated>pnpm --filter api test -- src/ai/observability/aiDebugDiagnostics.test.ts</automated>
  </verify>
  <done>Diagnostics contract is centralized, typed, and unit-tested rather than duplicated across route branches.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor recommend route to use centralized diagnostics builder on every non-production response</name>
  <files>api/src/routes/ai.ts</files>
  <action>Replace duplicated branch-local `debug` object literals with calls to the shared diagnostics helper while preserving existing non-production gating (`env.nodeEnv !== "production"`). Ensure all branches carry consistent baseline fields and include ranking/filter evidence where available, with explicit omissions for branches where data is not applicable (e.g., pre-retrieval clarify path).</action>
  <verify>
    <automated>pnpm --filter api build</automated>
  </verify>
  <done>All debug payload assembly is centralized and branch parity is maintained without production leakage.</done>
</task>

<task type="auto">
  <name>Task 3: Extend route-level regression assertions for diagnostics schema parity and ranking/filter evidence</name>
  <files>api/src/routes/ai.relevance-calibration.test.ts</files>
  <action>Augment relevance calibration route tests to assert the expanded diagnostics contract (including ranking-factor breakdown shape and filter evidence keys) and validate expected presence/absence semantics by branch outcome. Keep assertions stable and low-fragility by checking structural contracts over incidental timing values.</action>
  <verify>
    <automated>pnpm --filter api test -- src/routes/ai.relevance-calibration.test.ts</automated>
  </verify>
  <done>Route tests prevent future debug-schema drift and lock required non-production diagnostic evidence fields.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` passes after route diagnostics refactor.
- `pnpm --filter api test -- src/ai/observability/aiDebugDiagnostics.test.ts` passes for helper schema invariants.
- `pnpm --filter api test -- src/routes/ai.relevance-calibration.test.ts` passes with new diagnostics assertions.
</verification>

<success_criteria>
- Debug payload shape is consistent and centrally generated in non-production.
- Ranking and filter evidence fields are inspectable for QA/debug workflows.
- Contract drift risk from copied branch-local debug literals is eliminated.
</success_criteria>

<output>
After completion, create `.planning/phases/09-observability-and-regression-gates/09-observability-and-regression-gates-02-SUMMARY.md`
</output>
