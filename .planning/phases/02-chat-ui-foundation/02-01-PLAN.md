---
phase: 02-chat-ui-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/AiChat.tsx
  - frontend/src/features/ai-chat/model/chatTypes.ts
  - frontend/src/features/ai-chat/hooks/useChatSession.ts
  - frontend/src/features/ai-chat/components/ChatShell.tsx
  - frontend/src/features/ai-chat/components/ChatHeader.tsx
  - frontend/src/features/ai-chat/components/ChatFeed.tsx
  - frontend/src/features/ai-chat/components/ChatComposer.tsx
autonomous: true
requirements: [AIUI-01]
must_haves:
  truths:
    - "The AI chat experience is organized into explicit header, feed, and composer zones with stable ownership."
    - "Prompt submission and reset behavior are controlled by a single typed chat-session state contract instead of ad-hoc local booleans."
    - "The same foundational chat shell can render in both `/ai` route mode and embedded catalog mode."
  artifacts:
    - path: "frontend/src/features/ai-chat/model/chatTypes.ts"
      provides: "Canonical chat turn/request-state contracts shared by components and session hook."
      contains: "export type ChatRequestState"
    - path: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      provides: "Single orchestration hook for send/reset/deep-link flow while keeping existing API payload contract."
      exports: ["useChatSession"]
    - path: "frontend/src/features/ai-chat/components/ChatShell.tsx"
      provides: "Structural layout primitive that composes header/feed/composer zones."
      contains: "ChatShell"
    - path: "frontend/src/pages/AiChat.tsx"
      provides: "Thin route wrapper that wires shell + session for standalone and embedded usage."
      contains: "export default function AiChat"
  key_links:
    - from: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      to: "frontend/src/hooks/useAi.ts"
      via: "useAiCourseRecommendations mutation orchestration"
      pattern: "useAiCourseRecommendations|course-recommendations"
    - from: "frontend/src/pages/AiChat.tsx"
      to: "frontend/src/features/ai-chat/components/ChatShell.tsx"
      via: "wrapper renders shared shell with session props"
      pattern: "<ChatShell"
    - from: "frontend/src/pages/Catalog.tsx"
      to: "frontend/src/pages/AiChat.tsx"
      via: "embedded mode entrypoint"
      pattern: "<AiChat embedded"
---

<objective>
Create the structural foundation for the AI chat redesign by decomposing the current monolithic page into typed session/model primitives plus explicit layout zones.

Purpose: Establish maintainable contracts that Phase 2 visual/state work can build on without changing backend behavior.
Output: New `features/ai-chat` foundation files and a thin `AiChat` route wrapper that preserves existing request/response behavior.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-chat-ui-foundation/02-RESEARCH.md
@frontend/src/pages/AiChat.tsx
@frontend/src/hooks/useAi.ts
@frontend/src/pages/Catalog.tsx

<interfaces>
From frontend/src/hooks/useAi.ts:
```typescript
export type AiMessage = {
  role: "user" | "assistant";
  content: string;
};

export type AiCourseRecommendationsResponse = {
  assistantMessage: string;
  followUpQuestion: string | null;
  recommendations: AiCourseRecommendation[];
};

export function useAiCourseRecommendations();
```

From frontend/src/pages/AiChat.tsx:
```typescript
type AiChatProps = {
  embedded?: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define chat foundation contracts and shell component interfaces</name>
  <files>frontend/src/features/ai-chat/model/chatTypes.ts, frontend/src/features/ai-chat/components/ChatShell.tsx, frontend/src/features/ai-chat/components/ChatHeader.tsx, frontend/src/features/ai-chat/components/ChatFeed.tsx, frontend/src/features/ai-chat/components/ChatComposer.tsx</files>
  <action>Create typed contracts for chat turns, message roles, and request lifecycle (`idle/sending/success/error`) in `chatTypes.ts`, then define shell/component prop interfaces that make header/feed/composer ownership explicit. Keep this task structural: no final visual polish decisions and no API contract changes.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>All foundation components compile against a shared type contract and expose explicit props for zone composition.</done>
</task>

<task type="auto">
  <name>Task 2: Extract AI chat state orchestration into `useChatSession`</name>
  <files>frontend/src/features/ai-chat/hooks/useChatSession.ts, frontend/src/pages/AiChat.tsx</files>
  <action>Move prompt submission, API mutation orchestration, conversation turn accumulation, reset behavior, and deep-link `?prompt=` bootstrapping from the monolithic page into `useChatSession`. Preserve request payload shape for `POST /api/ai/course-recommendations`, keep existing localStorage preference loading, and return a deterministic session API for UI components.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>`AiChat.tsx` no longer owns orchestration internals; session behavior is centralized in a typed hook with unchanged backend compatibility.</done>
</task>

<task type="auto">
  <name>Task 3: Recompose `AiChat` around explicit header/feed/composer primitives</name>
  <files>frontend/src/pages/AiChat.tsx, frontend/src/features/ai-chat/components/ChatShell.tsx, frontend/src/features/ai-chat/components/ChatHeader.tsx, frontend/src/features/ai-chat/components/ChatFeed.tsx, frontend/src/features/ai-chat/components/ChatComposer.tsx</files>
  <action>Replace monolithic JSX in `AiChat.tsx` with composition through `ChatShell` and its zone primitives. Ensure both standalone route mode and `embedded` mode render through the same shell contract (only container strategy differs), and keep current suggestion-chip and basic send interaction behavior intact.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Route and embedded chat both render through shared primitives with explicit zone hierarchy and no API regression.</done>
</task>

</tasks>

<verification>
- Frontend build succeeds with new `features/ai-chat` files and updated imports.
- `/ai` route and catalog embedded AI mode both render using `ChatShell` composition.
- Existing AI request/response payload contract remains unchanged.
</verification>

<success_criteria>
- Chat layout ownership is explicit (header/feed/composer are distinct primitives).
- Session state orchestration is centralized in `useChatSession` with typed lifecycle.
- Foundation is stable enough for Phase 2 visual hierarchy and state presentation work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-chat-ui-foundation/02-chat-ui-foundation-01-SUMMARY.md`
</output>
