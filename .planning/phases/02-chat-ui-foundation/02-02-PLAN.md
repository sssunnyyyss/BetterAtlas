---
phase: 02-chat-ui-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/features/ai-chat/components/ChatFeed.tsx
  - frontend/src/features/ai-chat/components/ChatMessageBubble.tsx
  - frontend/src/features/ai-chat/components/ChatAssistantBlock.tsx
  - frontend/src/features/ai-chat/components/ChatRequestStatus.tsx
  - frontend/src/features/ai-chat/components/ChatComposer.tsx
  - frontend/src/features/ai-chat/components/ChatHeader.tsx
  - frontend/src/features/ai-chat/styles/chatTokens.ts
  - frontend/src/pages/AiChat.tsx
  - frontend/src/index.css
autonomous: true
requirements: [AIUI-02, AIUI-03]
must_haves:
  truths:
    - "User and assistant turns are immediately distinguishable through consistent role-specific visual hierarchy."
    - "Waiting, success, and failure states are explicit in the chat surface instead of implied from side effects."
    - "Assistant response text, recommendation cards, and follow-up prompt render as one coherent assistant-owned block."
  artifacts:
    - path: "frontend/src/features/ai-chat/components/ChatMessageBubble.tsx"
      provides: "Single role-driven message primitive with consistent user/assistant variants."
      contains: "ChatMessageBubble"
    - path: "frontend/src/features/ai-chat/components/ChatAssistantBlock.tsx"
      provides: "Grouped assistant presentation for narrative text, cards, and follow-up copy."
      contains: "ChatAssistantBlock"
    - path: "frontend/src/features/ai-chat/components/ChatRequestStatus.tsx"
      provides: "Explicit status UI for sending/success/error lifecycle states."
      contains: "ChatRequestStatus"
    - path: "frontend/src/features/ai-chat/styles/chatTokens.ts"
      provides: "Centralized role and status style tokens to prevent visual drift."
      exports: ["chatRoleTokens", "chatStatusTokens"]
  key_links:
    - from: "frontend/src/features/ai-chat/hooks/useChatSession.ts"
      to: "frontend/src/features/ai-chat/components/ChatRequestStatus.tsx"
      via: "requestState prop mapping"
      pattern: "idle|sending|success|error"
    - from: "frontend/src/features/ai-chat/components/ChatFeed.tsx"
      to: "frontend/src/features/ai-chat/components/ChatMessageBubble.tsx"
      via: "role-specific rendering"
      pattern: "role|user|assistant"
    - from: "frontend/src/features/ai-chat/components/ChatAssistantBlock.tsx"
      to: "recommendation card rendering"
      via: "assistant-owned grouping"
      pattern: "recommendations|followUp"
---

<objective>
Implement the visual hierarchy and explicit state presentation layer on top of the new chat foundation.

Purpose: Satisfy role clarity and lifecycle feedback requirements while preserving the current AI recommendation behavior.
Output: Role/status visual primitives wired into the AI chat shell with explicit waiting/success/error UI.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-chat-ui-foundation/02-RESEARCH.md
@.planning/phases/02-chat-ui-foundation/02-chat-ui-foundation-01-SUMMARY.md
@frontend/src/features/ai-chat/model/chatTypes.ts
@frontend/src/features/ai-chat/hooks/useChatSession.ts
@frontend/src/index.css

<interfaces>
From frontend/src/features/ai-chat/model/chatTypes.ts:
```typescript
export type ChatRequestState = "idle" | "sending" | "success" | "error";
export type ChatTurnRole = "user" | "assistant";
```

From frontend/src/features/ai-chat/hooks/useChatSession.ts:
```typescript
export function useChatSession(...): {
  turns: ChatTurn[];
  requestState: ChatRequestState;
  sendMessage: (text: string) => void;
  resetChat: () => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement role-driven message and assistant-group presentation primitives</name>
  <files>frontend/src/features/ai-chat/components/ChatMessageBubble.tsx, frontend/src/features/ai-chat/components/ChatAssistantBlock.tsx, frontend/src/features/ai-chat/components/ChatFeed.tsx, frontend/src/features/ai-chat/styles/chatTokens.ts</files>
  <action>Create reusable role/status token definitions and consume them in a single `ChatMessageBubble` component so user and assistant messages cannot diverge accidentally. Introduce `ChatAssistantBlock` to render assistant text, recommendation cards, and follow-up as one grouped unit; wire `ChatFeed` to use these primitives rather than inline duplicated markup.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Message rendering consistently uses role-specific primitives and assistant content is grouped as one visual block.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit waiting/success/error status surface and wire it to session lifecycle</name>
  <files>frontend/src/features/ai-chat/components/ChatRequestStatus.tsx, frontend/src/features/ai-chat/components/ChatFeed.tsx, frontend/src/features/ai-chat/components/ChatComposer.tsx, frontend/src/pages/AiChat.tsx</files>
  <action>Implement `ChatRequestStatus` that renders deterministic UI for `sending`, `success`, and `error` states. Connect status rendering to `useChatSession` request state so lifecycle communication is explicit and not inferred from timing. Keep retry interaction scope minimal in this phase (display-only clarity now, deeper recovery UX in Phase 3).</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Chat UI shows explicit pending/success/error states with consistent transitions tied to session state.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize hierarchy polish and composer-specific textarea behavior without global regressions</name>
  <files>frontend/src/features/ai-chat/components/ChatHeader.tsx, frontend/src/features/ai-chat/components/ChatComposer.tsx, frontend/src/pages/AiChat.tsx, frontend/src/index.css</files>
  <action>Apply hierarchy polish so header/context zone, feed zone, and composer zone remain visually distinct and consistent. Add a composer-specific textarea class override to neutralize the global `textarea` base rule (`min-height` and `resize`) inside chat only, keeping the rest of the app unchanged.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Hierarchy is visually unambiguous, and chat composer sizing/resize behavior is controlled locally without cross-app side effects.</done>
</task>

</tasks>

<verification>
- Frontend build passes with new role/status components and token module.
- Chat feed renders clear user vs assistant distinction for every turn.
- Status UI explicitly reflects sending, success, and error states in-session.
- Composer textarea behavior in chat is decoupled from global textarea defaults.
</verification>

<success_criteria>
- Users can visually distinguish message authors immediately and consistently.
- Users receive explicit feedback for waiting, success, and failure lifecycle states.
- Assistant responses present as cohesive blocks (text + cards + follow-up) with stable hierarchy.
</success_criteria>

<output>
After completion, create `.planning/phases/02-chat-ui-foundation/02-chat-ui-foundation-02-SUMMARY.md`
</output>
