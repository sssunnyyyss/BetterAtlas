---
phase: 02-chat-ui-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - frontend/src/features/ai-chat/components/ChatShell.tsx
  - frontend/src/features/ai-chat/components/ChatFeed.tsx
  - frontend/src/features/ai-chat/components/ChatComposer.tsx
  - frontend/src/pages/AiChat.tsx
  - frontend/src/pages/Catalog.tsx
  - frontend/src/pages/AiChat.foundation.test.tsx
  - frontend/src/test/utils/viewport.ts
  - .planning/phases/02-chat-ui-foundation/02-layout-qa-checklist.md
autonomous: true
requirements: [AIUI-04]
must_haves:
  truths:
    - "The redesigned chat layout works at target mobile and desktop breakpoints without clipping or zone overlap."
    - "Standalone `/ai` and embedded catalog AI mode follow one responsive layout contract with explicit container strategy differences."
    - "Automated tests lock core hierarchy/state behavior so future styling work cannot silently regress the foundation."
  artifacts:
    - path: "frontend/src/pages/AiChat.foundation.test.tsx"
      provides: "Automated structural/regression coverage for zones, role rendering, and request-state visibility."
      contains: "describe(\"AiChat foundation\")"
    - path: "frontend/src/test/utils/viewport.ts"
      provides: "Reusable viewport helper for breakpoint-oriented component tests."
      exports: ["setViewportSize"]
    - path: "frontend/src/features/ai-chat/components/ChatShell.tsx"
      provides: "Responsive container contract used by route and embedded chat surfaces."
      contains: "variant?: \"standalone\" | \"embedded\""
    - path: ".planning/phases/02-chat-ui-foundation/02-layout-qa-checklist.md"
      provides: "Manual breakpoint verification checklist for overlap/clipping confidence."
      contains: "390x844"
  key_links:
    - from: "frontend/src/pages/Catalog.tsx"
      to: "frontend/src/pages/AiChat.tsx"
      via: "embedded chat container sizing"
      pattern: "embedded|h-\\[calc\\(|min-h-0|overflow"
    - from: "frontend/src/pages/AiChat.foundation.test.tsx"
      to: "frontend/src/features/ai-chat/components/ChatShell.tsx"
      via: "zone-presence and layout assertions"
      pattern: "header|feed|composer"
    - from: "frontend/src/pages/AiChat.foundation.test.tsx"
      to: "frontend/src/features/ai-chat/components/ChatRequestStatus.tsx"
      via: "request-state visibility assertions"
      pattern: "sending|success|error"
---

<objective>
Harden responsive behavior and add validation coverage for the new chat foundation across standalone and embedded surfaces.

Purpose: Ensure Phase 2 foundation is stable at target breakpoints and protected by repeatable tests before interaction-polish work in Phase 3.
Output: Responsive layout contract fixes, `AiChat.foundation` automated tests, and a phase-local manual QA checklist artifact.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-chat-ui-foundation/02-RESEARCH.md
@.planning/phases/02-chat-ui-foundation/02-chat-ui-foundation-02-SUMMARY.md
@frontend/src/pages/AiChat.tsx
@frontend/src/pages/Catalog.tsx
@frontend/src/App.tsx
@frontend/vitest.config.ts

<interfaces>
From frontend/src/pages/AiChat.tsx:
```typescript
type AiChatProps = {
  embedded?: boolean;
};
```

From frontend/src/features/ai-chat/components/ChatShell.tsx:
```typescript
export type ChatShellVariant = "standalone" | "embedded";
export function ChatShell(props: ChatShellProps);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply responsive layout contract fixes for route and embedded chat containers</name>
  <files>frontend/src/features/ai-chat/components/ChatShell.tsx, frontend/src/features/ai-chat/components/ChatFeed.tsx, frontend/src/features/ai-chat/components/ChatComposer.tsx, frontend/src/pages/AiChat.tsx, frontend/src/pages/Catalog.tsx</files>
  <action>Replace fragile height assumptions with a clear container contract: `standalone` mode accounts for app shell (`Navbar`/`Footer`) without clipping, and `embedded` mode uses parent-constrained sizing without overflow collisions. Ensure feed keeps `min-h-0 + overflow-y-auto` behavior so composer remains visible at mobile/tablet/desktop breakpoints.</action>
  <verify>
    <automated>pnpm --filter frontend build</automated>
  </verify>
  <done>Both standalone and embedded chat surfaces respect responsive boundaries with no structural overlap regressions in code-level layout contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Add `AiChat.foundation` test suite with breakpoint helper coverage</name>
  <files>frontend/src/pages/AiChat.foundation.test.tsx, frontend/src/test/utils/viewport.ts</files>
  <action>Create a focused foundation suite that mocks `useAiCourseRecommendations` and asserts: header/feed/composer zone presence and ordering, user vs assistant visual-role differentiation, explicit sending/success/error status visibility, and breakpoint-specific layout expectations using a reusable viewport helper. Keep tests deterministic and fast.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/pages/AiChat.foundation.test.tsx</automated>
  </verify>
  <done>Automated tests fail on hierarchy/state/responsive regressions and pass on the intended Phase 2 foundation behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Create repeatable manual breakpoint QA checklist artifact for final overlap validation</name>
  <files>.planning/phases/02-chat-ui-foundation/02-layout-qa-checklist.md</files>
  <action>Author a concise manual QA checklist that validates `/ai` route mode and catalog embedded mode at 390x844, 768x1024, and 1280x800. Include exact steps, expected no-overlap/no-clipping outcomes for header/feed/composer, and where to record defects for follow-up fixes before phase completion.</action>
  <verify>
    <automated>pnpm --filter frontend test -- src/pages/AiChat.foundation.test.tsx</automated>
  </verify>
  <done>A durable manual QA artifact exists in the phase directory and aligns with automated coverage boundaries.</done>
</task>

</tasks>

<verification>
- Frontend build passes after responsive contract updates.
- `pnpm --filter frontend test -- src/pages/AiChat.foundation.test.tsx` passes.
- Checklist exists in phase directory and covers required breakpoints and both render modes.
</verification>

<success_criteria>
- Chat foundation behaves without clipping/overlap at the target breakpoints in standalone and embedded modes.
- Core zone, role, and status expectations are protected by automated tests.
- Manual QA checklist is ready for executor-driven breakpoint verification.
</success_criteria>

<output>
After completion, create `.planning/phases/02-chat-ui-foundation/02-chat-ui-foundation-03-SUMMARY.md`
</output>
